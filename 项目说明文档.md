# 项目说明文档

## 阅读导航

- [长期记忆计划](#长期记忆计划)
- [项目定位](#项目定位)
- [项目结构](#项目结构)
- [主流程](#主流程)
- [Talent Mapping](#talent-mapping)
- [平台行为](#平台行为)
- [筛选发现与订阅管理](#筛选发现与订阅管理)
- [内部运营控制台与智能助手](#内部运营控制台与智能助手)
- [运行模式](#运行模式)
- [运行要求](#运行要求)
- [运维入口](#运维入口)
- [架构说明](#架构说明)
- [数据模型与落盘结构](#数据模型与落盘结构)
- [邮件与失败语义](#邮件与失败语义)
- [维护建议](#维护建议)

## 长期记忆计划

本文档是 AutoRecruit 的长期项目记忆，记录经过代码和测试验证的当前能力、稳定契约、架构边界、数据语义、重要设计原因和已知限制。它不是开发日志、会话摘要或需求池；代码与测试仍是行为事实来源，Git 历史负责保留变化过程。

### 记忆边界

长期记忆应保留：

- 产品定位、支持范围和明确的非目标。
- 公共 CLI、平台隔离、任务队列、调度和高风险操作等稳定契约。
- 模块职责、关键调用链、共享类型和持久化数据之间的关系。
- 重要设计选择及原因，例如普通抓取/批量的 Boss 纳入必须显式 `includeBoss`、本地 JSON/JSONL 是事实来源、Qdrant 仅可重建。
- 成功、空结果、失败、重试、幂等和安全边界。
- 当前可用的控制台、API、前端入口及其与 CLI 的关系。
- 维护者恢复上下文时需要知道的源码和回归测试入口。

以下内容不进入长期记忆：

- API key、登录态、候选人数据、真实聊天内容和其他敏感信息。
- 单次任务进度、聊天会话摘要、调试日志、截图和临时排障结论。
- 尚未实施或未验证的设想；阶段计划完成后，只把仍然有效的结论归并到本文档。
- 易变化的完整 selector、筛选项、环境变量和命令目录；这些内容分别以源码、`.env.example` 和 `package.json` 为准。
- 可以从代码直接读出、且没有解释价值的逐文件或逐字段抄录。

### 信息权威顺序

发生冲突时按以下顺序处理：

1. 可执行代码、schema 和自动化测试决定实际行为。
2. 根目录及 scoped `AGENTS.md` 决定开发约束和不可破坏的合同。
3. 本文档解释当前产品能力、架构、数据语义和设计原因。
4. `README.md` 提供面向使用者的安装、启动和常用操作入口。
5. 阶段计划、issue、会话记录和 Git 历史只提供背景，不覆盖当前事实。

发现冲突时不能只修改文字使其“看起来一致”：先确认预期合同，再修正代码、测试或文档中的错误来源。未经验证的内容必须标记为待确认，不能写成当前能力。

### 更新触发条件

以下变更必须在同一工作项中同步本文档：

| 变更类型 | 必须检查的长期记忆区域 |
| --- | --- |
| 新增、删除或改变 CLI 模式、参数组合 | 项目定位、主流程、运行模式、失败语义 |
| 改变平台范围、搜索入口、浏览器复用或动作合同 | 平台行为、主流程、运行要求、维护建议 |
| 改变职位、候选人、运行、回执或 RAG 的落盘格式 | 数据模型与落盘结构、失败和重试语义 |
| 新增或修改 HTTP API、队列任务、调度能力 | 内部运营控制台、API 边界、架构说明 |
| 新增或重构前端页面和主要工作流 | 项目定位、控制台说明、项目结构、架构说明 |
| 改变安全、确认、身份复核或幂等行为 | 对应工作流、API 边界、失败语义 |
| 修改运行时版本、环境变量分组、构建或验证入口 | 运行要求、运维入口；使用示例同步到 `README.md` |
| 移动、重命名或删除核心模块 | 项目结构、架构说明、源码与回归入口链接 |

纯内部重构只有在不改变上述事实、边界和入口时可以不更新本文档，但完成前仍需检查是否产生陈旧路径或描述。

### 持续维护流程

每次功能或架构变更遵循同一个闭环：

1. 开始前按“根 `AGENTS.md` -> 本文档 -> scoped `AGENTS.md` -> 相关代码和测试”的顺序恢复上下文。
2. 实施中识别哪些变化属于稳定事实，哪些只是临时过程；只把稳定事实写入长期记忆。
3. 完成代码和测试后更新本文档的现有段落，使其描述新的当前状态，不在正文末尾追加流水账。
4. 同步检查 `README.md`、根和 scoped `AGENTS.md`，避免同一合同出现互相冲突的版本。
5. 使用源码、schema、`package.json`、`.env.example` 和测试反向核对名称、路径、命令与默认值。
6. 文档与对应实现一起评审和提交；阶段计划中的有效结论先归并，再把计划视为非权威历史材料。

### 责任与节奏

- 变更作者或执行该变更的代码代理负责同步长期记忆，不能把文档更新留给未定义的后续任务。
- 评审者重点检查合同是否与测试一致、设计原因是否清楚、旧描述是否被替换，以及是否写入敏感或短期信息。
- 行为变化采用事件驱动更新，每次变更都执行；每个发布节点再做一次全局审阅。
- 长期运行但发布不频繁时，每三个月检查一次文档中的路径、页面、命令、数据目录和测试入口。
- 不在正文维护手工“最后更新时间”；提交历史是时间记录，避免日期本身成为新的陈旧信息。

### 上下文恢复与完成标准

新维护者或新会话恢复项目时，先阅读本文档的项目定位、主流程、平台行为、控制台、架构、数据和失败语义，再进入与任务相关的 scoped instructions、实现和回归测试。只有需要理解某项设计的变化过程时才读取 Git 历史或阶段计划。

一次长期记忆更新完成前必须确认：

- 描述的是已经实现并验证的当前状态，而不是计划或推测。
- 同时说明“系统做什么”“边界是什么”，重要选择还说明“为什么”。
- 文档中的模块、路由、数据路径和测试入口真实存在。
- 已删除旧名称、旧路径和互相冲突的描述，而不是保留两个版本。
- 没有写入秘密、个人数据、运行产物或可被短期状态污染的内容。
- 验证范围与变更风险匹配，并在交付说明中明确实际运行过的检查。

## 项目定位

AutoRecruit 是一个 CLI-first、本地优先的招聘自动化工具。它面向招聘侧的重复执行场景，把平台搜索、候选人抓取、简历解析、JD 匹配评分、Markdown 报告和邮件发送串成可复跑、可审计、按平台隔离的数据流程。

项目当前包含七类能力：

- 普通简历抓取主流程：支持单职位、`--platform all`、`--jobs-file` 批量、保存搜索入口和直接筛选输入入口、已查看筛选切换、详情成功保存后评分、可开关的跨平台模型结果分流、Liepin 常用联系人转发、Zhilian 分享链接邮件交付；普通 `all` 可显式 `includeBoss`，将 Boss 直聘·直猎邦 Pro 作为第 4 阶段。
- 搜索条件维护流程：支持筛选发现、应用层筛选输入导出/校验、命名且版本固定的本地条件集、订阅管理结果数检查和显式保存平台搜索条件；条件集独立于 JD、候选人抓取、评分、报告和邮件。
- Talent Mapping：用版本化计划编排多公司/岗位搜索切片，对 51job、Liepin、Zhilian 做多批次卡片扫描，并按确定性规则补全目标详情，生成平台隔离的人才清单、公司岗位矩阵、覆盖和历次变化报告；人工审核跨平台实体线索和模型分类建议，不进入普通岗位的 seen、评分、交付或 RAG 链路。
- 内部运营控制台：提供本地 HTTP API 和 React 前端，用于查看任务、职位、候选人、人才地图、筛选 catalog、RAG 问答、运营诊断和循环自动运行；任务执行仍复用 CLI 语义、共享队列和本地数据目录。搜索相关新建任务与自动化计划使用服务端模式目录区分“按岗位设置抓取、订阅搜索、直接搜索、批量抓取、订阅管理”。
- 智能助手：在控制台中把自然语言整理成结构化任务草稿，支持普通抓取、批量、Talent Mapping、订阅管理、登录刷新、Boss 人才发现/打招呼/会话操作/职位同步、RAG 运营和候选人问答；确认后才进入现有后端执行路径。助手先选择稳定业务模式 ID，再由服务端派生兼容 task kind 和搜索来源；legacy 抓取省略来源时保持“不覆盖”语义且重复校验不改变输入。“订阅搜索”“直接搜索”“订阅管理”不能互相混用，冲突或未知模式只产生澄清；发送新消息会立即使旧草稿失效。Boss 普通抓取因可能复用岗位转发、报告邮件或模型分流配置，始终要求风险接受后才能确认。共享模式合同位于 `src/operation-modes.ts`，前端通过 `GET /api/operation-modes` 读取公开标签和声明影响。
- 离线导出工具：支持把已保存的结构化简历按本地 Word 模板导出为 DOCX，并在能确认详情页本人头像时放入模板头像位置。
- Boss 直猎邦 Pro 扩展：直接搜索/订阅搜索抓取、订阅管理和配置式转发可单平台运行，或在各自的 `all` 中显式纳入；推荐牛人、原生深度搜索、显式立即匹配、精确候选人打招呼、原子会话操作、职位/JD 同步和未读自动审查仍是 Boss 专属模式。查询默认只读；消耗匹配次数或联系候选人的动作需要显式确认；职位同步解决自动聊天依赖人工预存 JD 的问题。

当前浏览器平台分为三平台默认主流程和 Boss 直猎邦 Pro 扩展。普通 `--platform all` 默认只包含 51job、Liepin、Zhilian；普通抓取、batch 或订阅管理显式指定 `--include-boss true` 时，Boss 作为第 4 阶段加入。筛选发现、JD/RAG 问答和 Talent Mapping 的 `all` 保持三平台范围；Boss 专属模式仍必须使用 `--platform boss`：

| 平台 | CLI 名称 | 登录态文件 | 主要入口 | 核心规则 |
| --- | --- | --- | --- | --- |
| 前程无忧 | `51job` | `storage-state.json` | 订阅搜索页或直接人才搜索页 | 默认有头和可复用浏览器；保存入口通过订阅卡片进入并确认 `关键词：<keyword>`；直接入口清空筛选后输入关键词和应用筛选；默认勾选 `我已看` |
| 猎聘 | `liepin` | `storage-state.liepin.json` | 找人/快捷搜索或直接关键词搜索 | 默认有头和可复用浏览器，且始终强制有头；保存入口进入 `找人` 快捷搜索；直接入口清空筛选后输入关键词和应用筛选；默认勾选 `隐藏已查看`；可选常用联系人转发 |
| 智联招聘 | `zhilian` | `storage-state.zhilian.json` | `/app/search` 快捷搜索标签或直接关键词搜索 | 默认有头和可复用浏览器；保存入口点击快捷搜索标签并确认 `关键词：<keyword>`；直接入口不点快捷标签，清空筛选后输入关键词和应用筛选；默认勾选 `未看过`；普通抓取的交付动作自动复制分享链接 |
| Boss 直聘·直猎邦 Pro | `boss` | `storage-state.boss.json` | `https://www.zhipin.com/web/chat/search` 人才库、推荐/深度搜索、沟通或职位管理页 | 默认有头和可复用浏览器；普通抓取/batch/订阅管理可通过 `all + includeBoss` 在第 4 阶段运行，专属模式只允许单平台；普通抓取默认勾选“过滤近14天查看”，`--include-viewed true` 取消勾选；订阅管理只操作原生“我的订阅”；复用当前页面避免重复登录和新开标签；支持配置式转发、受控候选人联系、职位/JD 同步和独立自动沟通审查 |

项目不是通用 ATS，也不是完整 SaaS 或平台后台替代品。当前 Web 能力是本地内部运营控制台，重点仍是把已定义的本地运行模式稳定落页、保留可离线复盘的数据产物，并维持四个平台的适配边界以及 Boss 只有显式 opt-in 才进入普通 `all` 的产品约束。

## 项目结构

```text
autorecruit/
├── AGENTS.md
├── README.md
├── 项目说明文档.md
├── package.json
├── tsconfig.json
├── src/
│   ├── index.ts                  # CLI 参数解析与主编排
│   ├── operation-modes.ts        # CLI/服务端/控制台共享的稳定业务模式合同
│   ├── config.ts                 # 环境变量和运行配置
│   ├── types/                    # Job/Candidate/Score/RunResult、Mapping 及 Boss 专用类型
│   ├── platforms/                # 四个平台 facade、类型化动作入口、页面实现、纯解析，以及父/平台级 AGENTS.md
│   ├── browser/                  # 浏览器会话、节奏、简历解析和浏览器级 AGENTS.md
│   ├── search/                   # 筛选发现、应用层筛选、订阅管理
│   ├── talent-mapping/           # Mapping 计划、归一化、选择、存储、工作流、聚合、导出和 scoped AGENTS.md
│   ├── rag/                      # 本地 RAG 事实、检索、回答、质量运维和 RAG AGENTS.md
│   ├── extraction/               # 简历/候选提取校验与 Crawl4AI 接入
│   ├── parsers/                  # JD 解析
│   ├── scoring/                  # 评分 prompt、schema 和模型调用
│   ├── reporting/                # 报告聚合与邮件
│   ├── storage/                  # 本地 JSON 持久化
│   ├── llm/                      # 默认 OpenAI 兼容客户端与隔离 Codex 会话路由
│   ├── server/                   # 控制台、队列、调度器、智能助手和服务端 AGENTS.md
│   └── scripts/                  # 测试、调试、导出、迁移、验证脚本
├── frontend/                     # React 内部运营控制台和 scoped AGENTS.md
├── scripts/                      # Node TypeScript loader hooks
├── fixtures/                     # 脱敏筛选、订阅管理和 Talent Mapping 示例
├── docs/plan/                    # 历史计划文档
├── data/                         # 运行数据、职位、筛选 catalog、浏览器 profile
│   ├── <platform>/jobs/<jobKey>/
│   ├── <platform>/filter-catalog/
│   ├── talent-mapping/<mappingKey>/ # Mapping 观察、详情、运行、视图和导出
│   ├── boss/chat-review/         # Boss 自动沟通设置、结果与去重状态
│   ├── boss/chat-operations/     # Boss 原子会话变更幂等回执
│   ├── boss/job-sync/            # Boss 职位快照与同步运行
│   └── runtime/                  # 控制台任务、计划和计划轮次
├── storage-state*.json           # 平台登录态
└── resume-output/                # 简历渲染输出示例
```

核心边界：平台差异归属 `src/platforms/`；四个 `*-adapter.ts` 是 `PlatformAdapter` 注册和必要兼容导出的 facade，不直接操作页面控件，而是委托给 `src/platforms/<platform>/actions/` 中的类型化语义入口。平台自有的纯简历文本转换放在对应 `parsing/`，51job 已验证的启发式解析继续由 `src/browser/resume-detail.ts` 持有。Boss 的确认、身份复核、回执和固定流程仍由现有 `boss-*.ts` 工作流负责。主流程编排放在 `src/index.ts`；Talent Mapping 的独立计划、选择、持久化、聚合、变化对比、实体/分类审核和导出放在 `src/talent-mapping/`；无 selector 的共享 deadline context 和浏览器基础能力放在 `src/browser/`；搜索筛选与订阅编排放在 `src/search/`；普通岗位持久化放在 `src/storage/`。

当前页面动作模块化已完成 facade/API 边界和功能回归，Talent Mapping 新增的批次推进、批次变化验证和只读详情入口直接归属三平台候选/详情 action；但既有普通搜索实现层所有权尚未全部收口：Liepin 和 Zhilian 的若干导航、搜索、筛选、普通候选/详情入口仍从各自私有 `internal-page-actions.ts` 重导出，该 runtime 继续持有多个业务领域；51job 的普通搜索、候选列表和详情页面交互仍部分委托给 `src/browser/subscribe-search.ts`、`51job-search-subscription.ts`、`candidate-list.ts` 和 `resume-detail.ts`；Boss 的会话、人才、职位和大部分简历动作已分域，普通招聘的登录、搜索、筛选、候选提取和详情打开仍集中在 `boss/actions/search-actions.ts`。这些是已知架构限制，不改变现有 CLI 或 Mapping 运行语义；只有将剩余实现迁入对应领域 action、移除生产 adapter 对私有 runtime 的依赖，并增加直接动作回归后，才可视为全平台实现层模块化完成。

不同文档各自承担一种稳定职责，避免把易变化的命令、配置和代码约束重复维护在多处：

- 本地实现、设计和治理计划统一归档到 `docs/plan/`。该目录默认忽略，不是当前行为的权威来源；`npm run plan:init` 创建本地索引和模板，`npm run plan:new` 创建并登记规范文件，`npm run plan:check` 防止根目录、源码或其他文档目录重新出现计划文件。长期规则在根 [`AGENTS.md`](./AGENTS.md) 中维护。

- 代码约束采用“根级基线、领域级边界、平台级细节”的层次：根 AGENTS.md 对全仓库生效，最近路径上的 scoped 文档只增加或细化约束，不能静默放宽公共安全和平台合同。src/platforms/AGENTS.md 路由到四个平台文档；Talent Mapping、前端、浏览器、RAG 和服务端各有就近文档。npm run agents:check 校验这些路由、文件和基础章节，具体实现细节仍以代码和回归测试为准。

| 信息 | 权威入口 |
| --- | --- |
| 安装、登录和日常运行示例 | [`README.md`](./README.md) |
| 产品边界、流程、架构和落盘语义 | 本文档 |
| 完整环境变量模板 | [`.env.example`](./.env.example)；运行时默认值以 [`src/config.ts`](./src/config.ts) 为准 |
| 完整脚本目录 | [`package.json`](./package.json) 的 `scripts` 字段 |
| 仓库级代码约束 | [`AGENTS.md`](./AGENTS.md) |
| 平台、浏览器、RAG 和服务端专项约束 | [`src/platforms/AGENTS.md`](./src/platforms/AGENTS.md)、[`src/browser/AGENTS.md`](./src/browser/AGENTS.md)、[`src/rag/AGENTS.md`](./src/rag/AGENTS.md)、[`src/server/AGENTS.md`](./src/server/AGENTS.md) |

| AGENTS 分层结构 | 根 AGENTS.md、平台父/子文档、browser、talent-mapping、rag、server 和 frontend 目录下的 scoped AGENTS.md |

## 主流程

一次完整运行大致如下：

`CLI 参数 / jobs 文件` -> `JD 标准化` -> `JobRecord` -> `平台搜索` -> `候选人列表` -> `详情身份验证` -> `简历快照和结构化简历` -> `captured/seen-ids.json` -> `评分产物` -> `RunResult v2` -> `Markdown 报告` -> `邮件发送`

该链路描述普通抓取。Boss 另外提供四条彼此隔离的流程：

- 人才发现：`推荐或深度搜索页` -> `读取职位/核心要求/加分项` -> `可选精确同步条件` -> `默认只读返回卡片` -> `显式确认后才立即匹配` -> `按候选人 ID 可选单人打招呼`。
- 原子会话：`会话列表` -> `按 conversationId 打开并复核候选人/岗位` -> `只读消息/历史/简历，或显式确认一个变更动作` -> `变更成功写入 intentId 回执`。
- 职位/JD 同步：`职位管理列表` -> `按 Boss 职位 ID 打开详情` -> `读取 JD 并计算 hash` -> `未变化跳过 / 变化后解析` -> `更新平台隔离岗位记录` -> `同步运行摘要`。
- 自动沟通：`可选先同步职位/JD` -> `未读会话快照` -> `打开会话并判断此前是否聊过` -> `已聊过：只汇总本次新回复 / 首次沟通：按职位 ID 或唯一名称读取岗位 JD 和转发配置` -> `在线简历` -> `严格条件/评分判断` -> `符合者先转发、发求简历常用语并换电话 / 不符合者记录判断，并按显式开关决定是否发送拒绝话术` -> `审查结果` -> `总结邮件`。

具体语义：

1. 用户指定 `--platform` 和岗位关键词。普通抓取、batch 或订阅管理选择 `all` 时，只有明确提供 `--include-boss true` 才把 Boss 纳入；首次普通抓取新 `jobKey` 时还需要 `--jd` 或 `--jd-file`。Boss 已同步职位可额外提供稳定 `--boss-job-id`：它优先决定本地岗位身份，输入岗位名只用于身份复核；无 ID 时只允许唯一同名岗位回退。
2. 非 Boss 平台优先根据搜索关键词生成 `jobKey`。Boss 同步岗位以 `bossJobId` 生成稳定 jobKey，不能被页面搜索词或同名岗位替代。如果该平台下已有 `jd.json`，直接复用历史 JD 原文和标准化结果，不再读取或解析本次 JD 输入。该文件同时保存岗位搜索来源、direct 筛选输入及标准化条件、可选的条件集固定 revision 引用、独立 `pageKeyword` 和目录解析快照、报告邮箱、可选的跨平台模型结果分流策略，以及 Boss 主转发目标和版本 2 模型要求策略；同一配置只保留一份，不追加重复历史记录，也不重写内容完全相同的文件。
3. `all` 在任何浏览器动作前只读预检所有选中平台：核对已有岗位记录或本次 JD 来源，并在 direct 搜索时按每个平台的 catalog 校验同一筛选输入。预检失败会汇总报错且不打开任何浏览器。
4. Playwright 读取当前平台的 storage state，并验证登录态仍可访问平台已鉴权入口。
5. 进入平台搜索入口。用户可见名称“订阅搜索”对应内部 `--search-source saved`，“直接搜索”对应 `--search-source direct`；独立 `search-subscription` 任务是“订阅管理”，不进入候选抓取。CLI 搜索类代运行推荐通过 `npm run search:run -- --mode-id ...` 声明 `capture.reuse-job-settings`、`capture.subscription-search`、`capture.direct-search`、`batch.capture` 或 `subscription.manage`；历史 `npm run dev` 省略断言时保持兼容，但会在 stderr 输出隐式模式提示。新岗位默认使用订阅搜索，复用平台已保存订阅/快捷搜索；Boss 的 saved 来源必须有完整原生订阅引用，名称、职位范围、页面关键词、条件身份和条件指纹分开保存，缺少引用不会回退旧入口。已绑定且当前仍为 saved 的同一 Boss 岗位，显式选择 saved 时可以复用并重新验证该岗位的完整引用；direct 岗位中的残留引用永不复用。Boss 复用保存的 direct 条件集时，页面词依次取显式 `--boss-search-keyword`、保存的 `pageKeyword`、固定 revision 的 `defaultKeyword` 和岗位名；它与 jobKey 分离。直接搜索会清空旧条件、输入最终页面词、应用 legacy `--application-filter-input-file` 或固定 revision 的 `--search-condition-set`，再进入正常简历抓取。Boss 页面可能在筛选异步刷新时把短关键词替换成联想词，因此最终搜索按钮前会重新核对并恢复保存的页面词；城市选择确认也必须等待关闭后的省份摘要/选项状态提交完成，任一条件未验证都不会提交搜索。条件集是按平台隔离的独立本地事实，revision 不可变；岗位和任务保存引用及完整执行快照，后续编辑条件集不会追改历史运行。每次新引用、入队、调度轮次和浏览器启动前都重新依据当前 catalog 校验，字段/选项失效或编译语义变化直接失败。岗位首次保存 direct 条件后，后续只传平台和关键词即可复用；显式传入新的搜索来源或条件集 revision 时才替换历史搜索配置。服务端单岗位与 batch 任务将已解析 Boss ID、页面词、完整 saved reference、保存条件集 revision、主转发/主报告、模型要求策略和否定邮件副收件人/CC 固化为带 hash 的 settings v3/task v4 队列快照；显式空 CC 也作为精确值保存，排队后的岗位设置或策略文件编辑不会追改任务。
6. 对齐平台默认已查看筛选：51job 默认勾选 `我已看`，Liepin 默认勾选 `隐藏已查看`，Zhilian 默认勾选 `未看过`，Boss 默认勾选 `过滤近14天查看`；显式传入 `--include-viewed true` 时取消对应平台的页面筛选。51job 的切换是一个平台动作：若控件状态需要改变，动作先建立本次人才主结果请求和结果区观察，再执行带节奏的点击，并等待成功响应、loading 结束和结果稳定；控件本已满足时不重复点击，但仍等待正在进行的列表刷新稳定。51job 只读取推荐分界“未找到更多，为你推荐人才”之前的首个搜索结果列表；分界后的推荐简历不属于搜索结果，绝不进入候选快照、详情、seen、评分或交付。首个列表为空且该可见分界稳定时是明确的成功零结果；稳定空 DOM 或无关页面文案不是零结果证据。任何刷新证据、渲染或稳定性不足都在详情打开前失败，不能把旧卡片、临时空 DOM 或推荐卡片当作本轮候选。Boss 的控件只覆盖平台近 14 天已查看人选，不代表全部历史已查看；四个平台都继续以本地 `seen-ids.json` 过滤历史成功抓取，因此即使 `--include-viewed true`，本地已成功抓取候选人也不会重新进入详情抓取。加载中、iframe 未就绪或 Boss 明确加载错误则失败。
7. 普通 Boss 抓取在候选提取后先冻结原始顺序前 20 张卡片；第 21 张以后不恢复、不打开、不评分、不转发，也不补位。前 20 中已经存在于当前岗位经简历一致性校验的 `seen-ids.json` 的稳定 ID，每次运行必须完成一次“精确卡片定位 → 详情打开 → API 身份验证 → 停留 → 严格关闭”查看同步；有 pending-score 或可重试 outbox 的候选人由原业务详情生命周期覆盖，不能额外打开第二次。纯查看不解析、保存、评分、转发、联系、发邮件或改写 seen；身份/打开失败在安全关闭后可继续，关闭无法证明成功则停止后续卡片操作。Boss 在点击前以当前卡片稳定标识重新定位，列表序号仅用于诊断，不能单独授权点击。
8. 详情身份验证、解析后的候选人 ID 和目标 ID 三方一致，且简历文件落盘后回读身份一致，才算成功抓取；失败 ID 不写 seen，保持可重试。成功解析的简历会保存原始文本快照、DOM 快照和结构化 JSON。
9. 成功保存简历的候选人先写入 `seen-ids.json`，再进入评分阶段；详情打开或提取失败的候选人下次仍可重试。Boss 关闭模型要求分流时保持原有“转发、解析、seen、评分”顺序；开启结果分流时先落盘 pending-score 工作项和 seen，再从本地简历调用评分/模型要求判断，同时保持首次已验证详情打开且不执行页面动作。模型返回后使用新的有界页面动作预算重新核对同一候选人，在该详情继续处理或严格关闭；正常路径不为评分、检测或转发二次打开。中断或评分失败后可按平台/岗位/候选人 ID 和一致 policy hash 恢复，未完成工作不会被当作已有决定。
10. 评分从本地简历 JSON 执行。评分失败会写入带阶段诊断的失败评分产物，不回滚 seen，并保留 pending-score；它不是 `review`，不能创建 routing artifact、转发 outbox、否定邮件或聚合报告候选。只有成功评分后，证据不足/模型判断 `unknown` 才是 `review -> primary`，明确判断要求缺失是 `rejected -> secondary`，其余为 `qualified -> primary`。51job、猎聘只需稳定简历 ID；智联必须有唯一详情分享链接。猎聘原有站内联系人转发与 Boss 原生转发不由该分流策略接管。
11. 生成版本化轻量运行结果：v2 只写 `capturedCandidateIds`、评分/处理失败阶段和必要的分流事实，不再写歧义的 `newCandidateIds`；`captureAttemptCount` 只统计抓取路径。导出最新 Markdown 报告；如配置岗位报告收件人，则发送评分总结邮件。开启结果分流时，最新运行保存三组已决定候选 ID、未决评分 ID/脱敏阶段计数、策略 hash 和主报告快照；Boss rejected 另记录候选人级否定邮件状态，SMTP 前用同一策略 hash 的不可变 routing facts 校验互斥分组，主报告只含 qualified/review，rejected 不做 Boss 页面转发且不发送聚合副报告。主组为空不发送主报告；任一事实缺失、集合交叉、链接缺失或 hash 不一致均 fail closed。Boss 候选人简历转发目标、主报告收件人和否定邮件副收件人仍是独立配置，任一方都不会隐式启用另一方。

## Talent Mapping

Talent Mapping 是与普通职位抓取并列的独立模式，入口为 `--talent-mapping-file`。它复用平台登录、搜索准备、筛选回放、候选卡片和只读详情动作，但不复用 `runResumeCaptureFlow`，因此不会创建或更新岗位 `jd.json`、读写 `seen-ids.json`、评分、转发、联系候选人、发送邮件或写入 RAG。平台范围固定为 `51job`、`liepin`、`zhilian` 和三者组成的 `all`；Boss 明确不属于 Talent Mapping 产品范围，也不是后续 Mapping 里程碑。

运行由版本化 JSON plan 定义。Plan 包含稳定 `mappingKey`、研究目标、目标公司别名、岗位族/职级 taxonomy、多个搜索切片、三平台 search plan、每切片批次/候选/deadline 上限，以及 `card-only`、`targeted-detail` 或 `full-detail` enrichment policy。相对 `searchPlanFile` 从 Mapping 文件目录解析；选择 `all` 时，每个切片必须为三大平台提供计划或显式禁用并说明原因。Direct 条件出现 skipped/failed 时在读取候选卡片前失败，禁止从半应用筛选集合继续扫描。

两阶段语义如下：

1. `--mapping-stage scan` 只做卡片级市场扫描。切片是外层循环，平台是内层循环，`all` 严格按 `51job -> liepin -> zhilian`；任一搜索准备、筛选、分页或批次 postcondition 失败都会 fail-fast。每个批次保存 observation 和 checkpoint，只有页面/API 的明确空结果或分页终点才能产生完整扫描；候选、批次或 deadline 上限即使恰与终点同批出现，也标记为 `completed-with-gaps`。
2. 每次 scan/all 在开始前保存不可变的已校验 plan、`planHash`、`scanContractHash` 和范围指纹；运行记录引用 `runs/contracts/<runId>.json`。`--mapping-stage enrich` 从 `--mapping-run-id` 指定或最近一次成功 scan/all 运行读取本轮卡片观察；source run 必须覆盖本次选择的每个启用 slice/platform，且扫描合同必须与当前搜索/归一化合同一致，不能用单平台扫描伪装 `all` 补全，也不能以修改后的 plan 重新解释旧扫描。缺少合同的历史运行标记为 `legacy-unverifiable`，可读但不能作为严格补全来源。候选人按公司层级、岗位族、职级、地域、矩阵配额、平台排名和稳定 candidate ID 做确定性选择。它不调用模型评分；`targeted-detail` 是正式 Mapping 的推荐模式。
3. `--mapping-stage all` 在同一新 run 中先扫描再补全。只要计划需要详情，`enrich` 和 `all` 都必须在本轮显式提供 `--mapping-confirm-detail-open true`；历史配置不能授予权限。打开详情可能改变平台“已查看”状态，这一副作用写入运行摘要。
4. `full-detail` 只在 source scan 到达明确 end/empty 且平台结果与实际选择同时处于 `maxProfilesPerSlice` / `maxProfilesTotal` 硬上限内执行；source 被 capped 或数量超限时都在打开任何详情前拒绝。`card-only` 产物只能称为“市场扫描 / Mapping 初筛”，不能标记为完整 Mapping。

三平台通过 `PlatformAdapter` 暴露当前候选批次、带 expected batch identity 的下一批次推进和只读详情合同。翻页前验证当前批次身份，翻页后验证 fingerprint/页码确实变化；动作复用调用方 deadline、平台 pacing 和共享连续指针。Zhilian 的 modal 解析只读取简历，分享链接复制已拆到 `delivery-actions.ts`，仅普通抓取的 delivery hook 调用。51job/Zhilian 单候选详情失败可记录为 gap 后继续；Liepin 详情失败按现有检查语义停止并保留现场。

身份和完成口径：

- 候选人唯一键固定为 `platform:candidateId`。同一平台候选人在多个切片保留多条 observation，但派生视图按稳定平台身份聚合；不同平台永不根据姓名、公司、职位、学校或地域自动合并。
- 系统可以根据跨平台姓名一致及公司/岗位一致生成非权威 `possible-link` 线索，但线索不影响任何统计或执行。只有人工填写审核人和证据并确认后，才生成实体 ID 和“人工关联后实体数”；撤销保留原确认及撤销审计，平台原始档案始终独立。
- 达到批次、候选或 deadline 上限以及允许记录的详情缺口会产生 `completed-with-gaps`。TaskQueue 把它作为成功任务返回，但摘要和覆盖视图必须高亮 gaps；平台报正数结果却返回空卡片、缺少明确终点证据、搜索、筛选、批次身份或平台合同错误产生 `failed`。详情打开前使用当前卡片的精确 candidate ID、姓名、公司和职位证据复核，打开后再次核对详情 candidate ID；不一致只记录可重试 gap。
- 卡片和详情 observation 是本地 JSONL 事实；派生人才视图、公司岗位矩阵、覆盖 JSON、CSV 和 Markdown 可重建。失败运行保留已经写入的观察和 checkpoint，不伪装为 completed。
- 变化报告比较指定或最近两次成功 `scan`/`all` 的运行内观察。只有两次 `scanContractHash`、平台/切片/覆盖范围相同且均完整终止时状态为 `ready`，才显示新增、明确字段变化、本轮未再次观察及未变化档案；`partial`、`incomparable` 或 `insufficient` 只显示可核查观察差异和原因，不计算“未再次观察”。`not-observed-this-run` 不能解释为离职、跳槽或不再求职。
- 模型分类只生成待审核建议。输入限于截断后的当前公司、当前职位和由确定性规则确认的标准化地域，不发送姓名、平台候选 ID、卡片全文、简历或联系方式；输出值必须属于计划 taxonomy。接受时重新验证来源观察、证据原值、最新性和仍为空的目标字段，人工接受前后都不把模型建议伪装成页面事实。审核是 append-only 版本事件：重复同意幂等，冲突的同字段审核返回 `409`，已接受结果可填写审核人和原因后撤销或由新建议替代；所有实体关联、撤销和分类写入按 `mappingKey` 串行化并在持久化后重建派生物与导出。
- 详情保存原始文本快照只用于本地证据回溯，不自动进入分类模型或 RAG。快照文件名包含内容 SHA-256，重复 observation 会先检查幂等性；内容不同的重试保留原快照并写入本地冲突记录。导出默认不包含完整原始简历文本，并明确平台来源和跨平台未自动去重；CSV 将 `=`, `+`, `-`, `@` 开头的字段中和为文本，避免表格公式执行。

CLI 示例和脱敏 fixture 见 [`README.md`](./README.md#talent-mapping)。核心回归入口为 `npm run test:talent-mapping`，覆盖 plan、归一化/聚合、存储幂等、导出、工作流、CLI、TaskQueue/API、质量审核和详情确认合同；card-only 调度另由 scheduler 回归覆盖。

## 平台行为

### 浏览器操作节奏

网页动作节奏集中维护如下。除非表中另有说明，等待发生在导航、点击、输入、按键和候选人切换等浏览器操作之间；页面状态读取、简历解析、模型评分、文件写入和 SMTP 邮件不额外增加用户操作等待。

| 平台 | 动作间隔 | 候选人间隔 | 详情关闭规则 |
| --- | --- | --- | --- |
| 51job | 加权 `2000-4000ms`；约 80% 在 `2000-3000ms`、20% 在 `3001-4000ms` | 使用相同加权分布 | 详情就绪后停留一个动作间隔；处理完成后再等待一个动作间隔并关闭独立详情页 |
| Liepin | 使用相同加权分布 | 使用相同加权分布 | 详情就绪后停留，成功提取并保存后再等待并关闭；转发或提取失败时保留详情页 |
| Zhilian | 使用相同加权分布 | 使用相同加权分布 | 详情 modal 就绪后停留，处理完成后再等待并显式关闭 modal |
| Boss | 使用相同加权分布 | 使用相同加权分布 | 打开详情、详情停留、转发、关闭详情和进入下一候选人之间都经过共享节奏控制 |

平台级 `PLAYWRIGHT_<PLATFORM>_ACTION_DELAY_*` 和 `PLAYWRIGHT_<PLATFORM>_CANDIDATE_DELAY_*` 可以覆盖默认值。具体实现约束见 [`src/browser/AGENTS.md`](./src/browser/AGENTS.md)，平台动作边界见 [`src/platforms/AGENTS.md`](./src/platforms/AGENTS.md)。

鼠标轨迹在同一浏览器上下文中连续维护：每次点击从上一操作的实际落点出发，通过带中间控制点的分步路径移动到下一目标，页面或弹窗切换不会主动重置为固定坐标。移动不是无间隔事件突发：默认速度随机为 `700-1200 CSS px/s`，约每 `16px` 产生一步，并按起步/落点慢、中段快的时间曲线执行，短距离移动至少持续 `160ms`，长距离不会压缩时长。这段时间计入调用方已有 deadline；预算不足会在移动前失败，不会缩短为瞬移。必须保留 Playwright 原生点击或 DOM 事件点击的兼容路径，也会先把共享指针按同一速度移动到控件位置。导航、键盘输入和纯数据读取不伪造鼠标移动。

Boss 人才搜索关键词、直接聊天文本和备注在输入框获得焦点后逐字写入，默认字符间隔为随机 `80-180ms`，标点处增加短暂停顿；可通过 `PLAYWRIGHT_BOSS_TYPING_DELAY_MIN_MS/MAX_MS` 覆盖。更新搜索词和备注时先全选并删除旧值，直接聊天遇到非空草稿则停止而不覆盖。Boss 常用语仍通过页面选项点击，不进入逐字输入路径。

### 51job

51job 通过订阅搜索页进入人才搜索。流程会点击匹配的订阅关键词卡片，确认当前活跃订阅详情面板标题等于本次关键词，再点击该面板内的 `去搜索` 人才搜索入口；进入人才搜索页后还会确认页面显示 `关键词：<keyword>`，避免复用浏览器状态或其它订阅条件串入本次运行。流程保留多组 selector fallback，并把 `没有搜索到相关的人才` 这类明确空结果视为成功的 0 候选人运行。

候选卡片中的 `no_interested_<id>` 只作为候选人锚点，不作为详情入口。详情打开会优先点击候选人姓名、详情行等区域，并在单个点击入口无效时继续尝试其它 fallback，避免一个无效区域耗尽整次详情打开等待预算。

默认会显式勾选搜索页的 `我已看`；传入 `--include-viewed true` 时才会取消 `我已看`。两个方向都由同一个 typed viewed-policy action 处理：动作返回前必须证明当前筛选动作所属的主结果周期完成、已知 loading 均消失且结果在稳定窗口内未变化；若页面已是目标勾选状态，则保持幂等但同样不在加载或重绘中返回。候选读取随后经过 51job 平台 action 的稳定快照：只读取“未找到更多，为你推荐人才”分界之前的首个搜索结果列表，读取前后复核该列表身份、loading 和结果 generation；分界后的推荐卡片永不进入普通抓取。首个列表为空且可见分界稳定时返回成功零结果，任何变化或缺少明确空结果证据都在同一搜索 deadline 内重新等待或失败，而不是把混合、旧卡片或推荐卡片交给详情流程。普通抓取不再经 legacy page-extraction 旁路；离线 HTML/source 转换和纯提取校验仍可独立复用。筛选刷新或稳定快照失败不会打开详情、写入 seen、保存简历、评分或触发报告/邮件，失败候选保持可重试。51job 默认启用可复用浏览器；打开人才搜索页后会关闭同一上下文里多余的“我的订阅”页面，避免复跑时订阅页标签堆积。

### Liepin

Liepin 始终使用有头浏览器，`PLAYWRIGHT_HEADLESS=true` 对 Liepin 不生效。浏览器启动引擎默认是 CloakBrowser；如需回退到 Playwright 自带 Chromium，可设置 `BROWSER_ENGINE=playwright`。Liepin 默认启用可复用浏览器：流程结束会关闭最后的详情页、停留并置前招聘端人才搜索页，下次运行优先通过本地 CDP 端口连接这个窗口继续。

搜索流程必须进入招聘端人才搜索面，必要时从首页点击顶部导航 `找人`，再点击本次关键词对应的快捷搜索标签。默认确认 `隐藏已查看` 已勾选；传入 `--include-viewed true` 时才取消该筛选。勾选或取消已查看筛选后，旧的 `search-resumes` 响应不能复用，候选提取以 DOM 为先，只有 DOM 无候选或缺安全详情 URL 时才短等 API fallback。

Liepin 主流程支持 `--liepin-forward-contact "<常用联系人姓名>"`。该开关只处理本地 `seen-ids.json` 过滤后的新增候选人：打开详情页后先点击 `转发`，在弹窗里选择联系人并确认，成功后才解析简历和标记 seen；转发失败时该候选人保持可重试。

Liepin 站内操作、候选人切换和成功详情关闭遵循本节统一的[浏览器操作节奏](#浏览器操作节奏)，点击会沿共享连续轨迹移动鼠标到目标元素。转发或提取失败时保留详情页供检查。

### Zhilian

Zhilian 登录页是 `https://passport.zhaopin.com/org/login`，已鉴权搜索页是 `https://rd6.zhaopin.com/app/search`；`https://rd6.zhaopin.com/desktop` 不是搜索流程目标页。

Zhilian 默认启用可复用浏览器。搜索必须点击已保存的快捷搜索标签，标签文本需要包含本次运行传入的原始 `--keyword`，点击后还必须确认条件中出现 `关键词：<keyword>`。默认会显式勾选 `未看过`；传入 `--include-viewed true` 时只取消可见的 `未看过`，不改动 `未聊过`。如果取消或勾选后快捷搜索条件消失，流程会重新选择该快捷搜索标签，再次设置 `未看过` 并重新确认关键词条件。

候选列表提取 DOM-first。当前搜索卡片可能没有可用详情链接或显式 ID，因此 DOM 提取会优先读取卡片 Vue props 中的 `userMasterId`、`resumeNumber`、`resumeK`、`resumeT` 等字段；DOM 无候选时才等待 API fallback 或明确空结果。

Zhilian 简历详情是 `/app/search` 页内 modal，不是新标签页。纯详情解析只读取 modal 子树，不点击分享或交付控件；普通抓取在独立 delivery action 中通过 `转给同事` -> `链接转发` 复制分享链接，再把它合并为 `candidateShareUrl`。该详情生命周期使用平台估算的单一 deadline 并预留关闭预算：复制观察在点击前安装，链接结果可在剩余预算内从拦截器、系统剪贴板或可见页面读取；转发层只能点击其唯一关闭控件，并在返回解析前证明底层同一简历 modal 仍然存在，不能使用会连同简历一起关闭的 Escape。解析等待沿用该生命周期剩余预算，不再另设固定一秒窗口。链接缺失归类为可重试的 `forward` 失败，详情恢复或解析失败归类为 `parse`，均不写 seen。Talent Mapping 只调用纯读取路径。Zhilian 有新增候选人的普通抓取报告邮件必须使用当前运行复制出的唯一分享链接；缺失或重复会作为邮件发送错误记录。

### Boss

Boss 登录入口是 `https://www.zhipin.com/web/user/?ka=header-login`，默认使用 `storage-state.boss.json`、独立浏览器 profile 和本地 CDP 端口 `19331`。运行时优先复用已经打开的 Boss 搜索、推荐/深度搜索、沟通或职位管理页；不要反复访问登录页，也不要为每次任务创建新 Boss 标签。登录态缺失或失效时，才显式运行登录保存命令并由用户完成登录。
有头 Boss 会话在认证页面准备完成后调用一次页面置前，使本轮复用的 Boss 标签在流程开始时进入前台；无头模式不执行，置前失败仅告警且不改变招聘任务结果。它不持续占用系统最前层，用户后续可正常切换窗口。

#### 普通抓取

Boss 普通抓取支持稳定岗位定位、当前搜索页准备、direct 条件回放、候选人列表/简历解析和配置式转发。同步岗位优先用 `bossJobId` 定位，名称回退必须唯一；jobKey 只决定 JD、seen、简历、评分和报告归属，页面查询使用独立 `pageKeyword`。保存的 direct 条件集每次都以固定 revision 重新校验并在候选提取前完成语义验证；其默认关键词可作为页面词，但不能变更岗位身份。TaskQueue 对复用岗位设置的任务持久化 Boss ID、实际页面词和 revision 快照，调度每轮重新解析再入队。每次普通 Boss 抓取只保留平台结果顺序中的前 20 份简历，并在 seen、恢复、详情、评分、转发和持久化前截断；前 20 份中的 seen 不会用第 21 份以后补位。转发参数出现时必须成对提供：`--boss-forward-mode colleague|email` 与 `--boss-forward-recipient`；邮件模式可另传 `--boss-forward-cc`，其值规范化后写入 `bossForwarding.ccEmails`，站内同事模式不接受非空 CC。显式配置只保存到当前岗位记录，后续同岗位普通抓取可省略并复用；普通抓取不得改写自动沟通的全局默认，只有自动沟通模式自身的显式配置可以更新该默认及其邮件转发 CC。站内同事模式填写姓名并选择唯一匹配项。Boss 邮件转发框没有原生 CC：先向主地址发送，再为每个去重后的 CC 地址重新打开一个新转发框独立发送；每一次留言都写入并校验同一个候选人 ID。开启模型要求分流且 qualified/review 使用邮件转发时，当前详情如存在同事沟通记录，每个留言再追加一行 `同事已沟通`；系统只读取布尔状态，不读取或持久化同事姓名、时间和沟通详情。

搜索简历详情同时兼容旧 `/web/frame/c-resume/` canvas 和当前父页面 `.dialog-lib-resume` 原生 Vue 布局。原生详情以当前组件实例的 `resumeInfo.expectId` 和完整简历字段作为身份/解析事实，不读取可能属于前一个候选人的父页面 performance 历史；旧布局仍只读取当前详情 iframe 内的 API。原生详情底部自身的“搜索畅聊卡”是联系额度入口，不会被误判为购买弹窗。当前转发入口是基础信息顶部收藏、不合适、举报、转发按钮组最右侧唯一的“转发牛人”，点击前会重新验证整组顺序和唯一性；旧 `.btn-coop-forward` 仅作旧布局兼容。转发按钮和最终确认都使用指针移动后的新鲜 locator 点击，避免旧坐标落到“联系 Ta”。当前转发弹窗没有关闭按钮，严格清理只点击经几何与命中验证的唯一遮罩空白点，并证明底层简历仍在；不能使用会关闭底层简历却遗留转发框的 Escape。若真正出现搜索畅聊卡购买框，动作会关闭它并在任何转发确认前失败；详情卡片点击本身出现购买框也会关闭后立即终止当前页面会话，不继续下一张卡片。

Boss 开启模型要求分流时，正常路径使用一个详情生命周期和两个有界页面动作阶段：首次阶段完成身份校验、解析及 pending/seen 持久化；随后从本地简历等待模型时保持该详情打开，但不执行页面动作。模型返回后建立新的有界 continuation，立即重新核对同一稳定候选人。qualified/review 在原详情内持久化决定/outbox、按需读取同事沟通布尔状态并完成所有主地址转发，再严格关闭；rejected 和评分失败不读取沟通状态、不调用页面转发，并直接严格关闭。rejected 只有在关闭成功且关闭凭证落盘后才创建候选级 SMTP outbox。后续运行恢复已持久化的 retryable 转发 outbox 时可按稳定 ID 独立打开详情一次，但这不是正常路径的二次打开。页面动作在消耗用户式节奏前为关闭预留预算，节奏结束后重新计算剩余超时，并在共享指针移动后再次核对稳定候选人/控件身份。确认转发只有在 click event 已派发且出现新的可见 Boss 成功提示时才记为 sent；点击前失败记为可重试，点击已派发但无成功证据记为 uncertain，后者不自动重发。

Boss 每次真正执行 saved、direct 或条件集搜索时，都在职位、筛选项、viewed 策略和关键词输入完成并通过最终条件核验后，显式点击一次唯一、可见且可用的全局搜索按钮；即使所有字段已满足、没有字段变化，也不能跳过这次点击。direct 搜索将关键词放在其他筛选稳定之后，只输入一次，避免页面刷新把短关键词替换成自动建议词后再次输入；关键词输入不使用 Enter 提前提交，筛选控件触发的页面自动刷新也不计作本轮最终提交。点击后必须观察到属于本次点击的新搜索周期（结果列表变化、loading 周期或可识别搜索资源），并再次核对条件后才读取候选卡片；旧卡片或旧空态不能作为证据。按钮缺失、歧义、条件失败、取消或点击后结果周期不可观察时 fail closed，点击结果不确定时不自动再次点击。页面准备、筛选发现、单字段应用、重置和恢复动作保持不提交。

岗位可选 `bossScreening` 开启仅适用于普通抓取的评分后模型要求分流。策略版本 2 只允许 `modelRequirement`，用
`requirement`、`criteria` 和 `insufficientEvidence` 描述正向业务要求；模型在同一次 Boss 评分请求中返回
`satisfied | missing | unknown`。通用箱包、皮具、女包、公司名称或岗位名称不能单独证明铝制箱包能力。`satisfied`
必须提供当前结构化简历中的逐字证据；`missing` 进入候选人级否定邮件；成功评估中的 `unknown` 转主受众复核。连接、进程、协议、模型输出或 schema/证据校验失败保持 pending，不形成受众。
最终受众仍由纯代码决定。关闭或缺少策略时，保持“转发、解析、seen、评分”，转发/解析失败仍可重试。开启时，成功
解析保存后先写 pending-score 和 seen，在同一详情打开时评分和判定；决定、policy hash 和交付 outbox 在外部动作前持久化，随后
`qualified/review` 在该精确候选详情内检测同事沟通状态并只使用主转发，`rejected` 不检测且不调用任何 Boss 页面转发，而在严格关闭后使用已验证关闭事实向
`secondaryDelivery.recipientEmail` 逐候选发送一封包含全部否定原因和完整结构化简历的邮件。页面转发 outbox 为主地址和
每个 CC 独立保存 `pending/sending/sent/retryable-failed/uncertain/superseded`；否定邮件有独立
`routing/rejection-email-outbox/`，以 `jobKey + candidateId + delivery contract` 去重，并保存最终主题、正文、content hash 和
稳定 Message-ID。只有 `pending/retryable-failed` 且已记录详情关闭的邮件可自动恢复；`sent/uncertain` 永不自动重发。
为防止进程中断后重复投递，SMTP 调用前先预占并保存可选 `attemptCount`，同时保存结构化失败阶段和一次性重试授权；进程若在
预占后、调用前中断，该槽位也会保守地视为已消耗。只有 `EDNS + CONN` 能证明 DNS 连接前失败并
在当前运行短暂退避后重试一次，AUTH/MAIL 只保留下一轮的一次机会；第二次仍证明未提交时标记 `retryExhausted`，不再自动恢复。
Nodemailer 可能在 DATA 后仍把 socket timeout/close 记为 `command=CONN`，因此 ETIMEDOUT、ESOCKET、ECONNECTION、其他
CONN、RCPT、DATA、阶段未知、部分收件人结果、遗留 `sending` 以及只有错误文本而无结构化证据的失败都保持 `uncertain`，
历史 `uncertain` 不回退。同一 delivery 的状态读写和 SMTP 调用由原子文件锁串行化，锁内重新读取最新 outbox，跨进程也不得
超过两次调用。
严格关闭动作成功后，编排会在详情生命周期返回前持久化 `detailClosedAt`，SMTP 位于生命周期返回之后；关闭凭证写入失败时本轮
直接停止且 SMTP 调用为零。恢复只使用 outbox 的完整简历与分流事实，不重新打开详情，也不依赖候选人仍在前 20 或尚未进入
历史 run；本轮检查过的恢复终态会进入当前 RunResult 和失败归约。没有关闭凭证的邮件保持不发送并显式报告，进程遗留的
`sending` 转为 `uncertain` 后要求人工核对。
版本 1 以及旧 `scoreBelow`、`resumeFact`、`resumeMissingKeywords` 不再兼容执行，必须在浏览器和 SMTP 前迁移或失败。

Boss 分流命令/任务摘要通过 `reportDeliveries.primary/secondary` 返回聚合报告事实，并通过 `rejectionEmails` 返回候选人级
否定邮件状态计数、不可变 `deliveryTargets` 及失败候选 ID，并覆盖 `pending/sending/sent/retryable-failed/uncertain/superseded`。
新合同的 secondary 聚合报告固定跳过；兼容字段 `emailAttempted` 汇总主报告和必要的候选人邮件，
`emailDelivered` 只在所有非跳过的应发邮件都成功时为真；`rejectionEmailSmtpAttemptCount` 记录当前运行真正调用
`sendMail()` 的次数，预检失败和历史累计次数不会被误算为本轮尝试。
任务详情会解包单平台、batch 和 all+Boss 子结果，显示显式输入或不可变快照中的副收件人/CC、全部候选人级状态和
`sending/uncertain/retryExhausted` 人工核对提示，并将 `retryExhausted` 排除在下一轮自动恢复之外；Dashboard health 只汇总 Boss 否定邮件 outbox 状态数量，
不返回邮件正文或 SMTP 凭据。
主聚合报告标题固定为 `【BOSS】<岗位> 评分结果（成功数/候选人数）（主）`，新合同不生成 rejected 聚合副报告。
否定邮件正文由独立完整渲染器生成，每位候选人一封，包含全部明确缺失要求、理由、核验证据和完整结构化简历；不复用
聚合报告的截断正文。完整评分仍由普通本地导出保存，非 Boss 分流邮件继续使用原有完整报告格式。

HTTP、助手、batch 和 scheduler 在入队边界生成 Boss settings v3/task v4 任务快照，固化岗位身份、完整搜索计划、交付/筛选目标和
配置 revision；执行时以 CAS 应用显式 patch，revision 冲突在浏览器和 SMTP 前失败。RunResult 保存本轮不可变报告
收件人/抄送，手工补发按最新运行事实读取，不会因岗位后续编辑而重新分流或改投地址。旧 v1 `newCandidateIds` 只作为
尝试历史，不能计入成功抓取。策略迁移同时处理旧 policy 的页面转发与否定邮件 outbox：未开始或已确认未调用 SMTP 的旧邮件
转 `superseded`，旧 `sending` 转 `uncertain`，终态不回退。合同迁移只审计 `queued/running` 任务、数据目录内的不可变 batch
快照和 schedule 模板；终态任务保留审计但不阻断，也不会跟随任意外部 jobs-file。

Boss 普通抓取、人才发现/联系、原子会话、职位同步和自动沟通共同遵循本节统一的[浏览器操作节奏](#浏览器操作节奏)，不得引入无间隔的导航、点击、输入、按键、转发或候选人切换动作。

#### 自动沟通

Boss 自动沟通是独立模式。流程只点击左侧“沟通”进入 `https://www.zhipin.com/web/chat/index`，选择“未读”，先快照所有带红点的会话，再按会话 ID 逐个打开。

- 自动沟通模式自身显式提供的转发和总结邮件配置会去重保存到 `data/boss/chat-review/automation-settings.json`；
  邮件简历转发复用 `--boss-forward-cc`，且仍只允许 email 模式；
  普通抓取的岗位级转发配置不会写入该文件。
- 首次沟通候选人的转发目标按“岗位记录优先、Boss 平台默认其次”复用。
- 首次沟通缺少岗位 JD 或转发配置时，在打开并完成历史沟通判断后记录可重试失败，不加入已审查 ID。
- 已聊过候选人不需要岗位 JD 或转发配置。
- 可选 `--boss-sync-jobs-before-review true` 会在读取未读会话前同步职位/JD；任一同步项失败时本轮审查立即停止。
- 会话有 Boss 职位 ID 时必须优先按 ID 找岗位记录；没有 ID 时只允许回退到唯一的标准化同名岗位，同名多岗位必须报歧义错误。

在线简历入口必须使用 Playwright 原生 locator 点击。会话切换后还要等待 Vue 当前会话 ID 和候选人基础信息（包括 `ageDesc`）完成异步水合，再读取摘要；简历详情可合并 iframe `IFRAME_DONE` 的 WASM 解密数据，但原始解密载荷不会落盘。

#### 物业电工严格规则

`--boss-chat-require-all true` 要求简历对六项条件提供明确证据：

1. 年龄严格小于 47 岁。
2. 明确持有高压电工证。
3. 明确持有低压电工证。
4. 明确具有物业行业电工经验。
5. 至少一家公司连续工作 24 个月。
6. 明确是上海人；期望城市或现居地为上海不能替代籍贯证据。

任一项缺少明确证据都判为不符合，不做推测。只有其他五项全部满足、简历没有明确上海或非上海籍贯且教育经历显示在上海学校就读时，才允许关闭简历后询问 `是上海人吗？`。此时状态记为等待确认，不转发、不拒绝、不换电话，也不加入已审查 ID；上海就读经历本身不算上海人证据。

#### 消息与跟进回复

符合候选人的动作顺序固定为：保持简历浮窗打开并先完成转发；立即记录 `forwarded: true`；按统一节奏关闭简历浮窗；从常用语选择 `方便发一份你的简历过来吗？` 并发送；点击“换电话”并确认。

不符合者不转发、不换电话，关闭简历浮窗后记录判断。默认不发送消息；只有显式设置 `--boss-chat-reply-unqualified true`，才从页面常用语选择完整话术 `对不起，看了你的简历以后觉得不太合适，希望你早日找到满意的工作机会` 并发送。符合和不符合候选人的预设消息都禁止直接填写编辑器，具体 selector 由平台适配层维护。

上海籍待确认候选人是受控例外：直接在聊天框输入 `是上海人吗？` 后点击主发送按钮；若编辑器已有其他草稿则禁止覆盖。消息和换电话有幂等保护；转发成功后联系失败会保留转发成功状态，不自动重复转发。

点击红点候选人后，流程同时等待当前会话、候选人基础信息和消息列表加载，再记录 `previousChat`。证据优先级依次为 Boss `bothTalked` 状态、当前可见的招聘方历史消息，以及“可见消息数大于点击前保存的未读数”的兜底判断。

若此前已聊过，只按时间顺序提取最后 `unreadCount` 条候选人消息作为 `newCandidateReplies`，状态记为 `follow_up_reply`，随后立即结束该会话；不会打开简历、读取或判断 JD、评分、转发、发送话术或换电话。文本会规范空白，非文本消息使用明确类型占位符；发送方或消息数量无法可靠确认时记录可重试失败，不生成空白回复。

尚未成功转发的失败会话会从历史审查运行中恢复。真实的新红点事件不会因为会话 ID 已存在于 `reviewedConversationIds` 而跳过；该列表只阻止没有新红点的失败恢复项被重复执行。已聊过候选人的新回复在总结邮件中单独展示，汇总同时统计首次沟通数、此前已聊过数、跟进回复会话数和新回复消息数。运行结束保持当前 Boss 沟通页打开。

#### 推荐、深度搜索与单人打招呼

推荐牛人入口是 `/web/chat/recommend`（候选人列表可能位于 `recommendFrame`），原生深度搜索入口是 `/web/chat/aiform`。两种查询默认只读，并统一输出稳定候选人 ID、卡片摘要、联系状态和来源；不得把可见序号当成执行标识。

深度搜索读取并可精确同步“核心要求”和“加分项”。至少需要一项核心要求；`triggerMatch` 默认 `false`，只有显式确认后才可点击“立即匹配”，且必须先确认剩余次数大于零、按钮可用。匹配结果最多返回最新 20 位候选人。单人打招呼执行前必须按候选人 ID 重新定位卡片，并复核预期姓名、职位名和可用的 Boss 职位 ID；卡片已显示“继续沟通”时视为已联系，不重复点击。

#### 原子会话操作

Boss 会话支持只读操作 `list-conversations`、`open-conversation`、`read-conversation`、`read-history`、`preview-resume`，以及受控变更操作 `send-text`、`remark`、`mark-not-fit`、`request-attachment-resume`、`accept-attachment-resume`、`exchange-phone`、`exchange-wechat`。所有 HTTP 和控制台请求必须进入共享 `TaskQueue`，再复用 Boss 当前浏览器 profile/CDP/标签页；路由层不得直接操作浏览器。

变更操作必须提供精确会话 ID、`confirmed=true` 和唯一 `intentId`。打开后再次校验候选人和职位；通用文本发送发现编辑器已有草稿时必须失败，禁止覆盖。成功回执落在 `data/boss/chat-operations/runs/`，同一 `intentId` 重试返回原回执。原子变更操作不允许加入调度计划。

#### 职位/JD 同步

职位列表入口是 `/web/chat/job/list`。同步包含开放、审核中和已关闭职位，因为未读会话可能引用已关闭岗位；也可显式排除已关闭职位或只同步指定 Boss 职位 ID。岗位目录按“职位名 + Boss 职位 ID”生成稳定 `jobKey`，同名不同 ID 永不合并。

同步结果写入 `data/boss/job-sync/positions.latest.json` 和 `data/boss/job-sync/runs/`，有效 JD 仍写入标准 `data/boss/jobs/<jobKey>/jd.json`。Boss 同步不调用模型：职位名、地点、部门、状态及页面薪资直接取自已复核页面，JD 自由文本只按明确章节、列表和完整数值原句做保守规则解析；无证据字段保持为空。JD 原文先计算 SHA-256，且哈希与规则版本均未变化时不重写岗位记录；旧记录会在下一次同步时惰性重建为当前规则结果。读取或解析失败不会覆盖已有有效 JD，原有创建时间、转发、报告和搜索配置会保留。

## 筛选发现与订阅管理

本节只描述搜索筛选项发现、应用层筛选输入和订阅管理结果数检查。这些能力不解析 JD、不创建职位记录、不抓候选人、不评分、不导出报告、不发送邮件，也不改 seen 状态。

### 筛选发现

筛选发现入口：

```bash
npm run discover:filters -- --platform <51job|liepin|zhilian|boss|all> --keyword "<关键词>"
```

`--platform all` 按 `51job -> liepin -> zhilian` 串行执行，任一平台失败立即停止；Boss 发现必须单独使用 `--platform boss`。发现结果写入：

- `data/<platform>/filter-catalog/latest.json`
- `data/<platform>/filter-catalog/<timestamp>.json`

Liepin 和 Zhilian 默认采用较保守的静态读取；需要打开下拉、弹层和级联树时，显式传入 `--slow-click true`。Zhilian 发现必须停留在 `/app/search`，复用保存的快捷搜索标签并确认搜索条件面板；不要用泛文本点击 `筛选` 或 `使用高级搜索`，这些文字可能命中推荐页或顶部导航。

Liepin 行业完整树单独采集：

```bash
npm run discover:liepin-industry-tree -- --keyword "优衣库"
```

该命令采集 `当前行业`、`期望行业` 的完整大类/叶子树，并合并回 `latest.json` 与 `application-filter-options.latest.json`。行业父级只作为 `pathLabels` 路径节点，叶子才是可提交值。

### 应用层筛选输入

筛选 catalog 可以导出成应用层 options：

```bash
node --import ./scripts/node-ts-hooks.mjs src/scripts/export-application-filter-options.ts <platform>
```

导出文件是 `data/<platform>/filter-catalog/application-filter-options.latest.json`。应用层用 `groups.singleSelect`、`groups.textInput`、`groups.salaryRange`、`groups.numberRange` 分组；`fieldsById.<fieldId>.kind` 决定输入形态。

常见输入形态：

- 普通单选：`"education": "本科"`
- 文本/弹层输入：`"company_name": "迅销"`
- 级联或同名叶子：`{ "value": "电子商务", "pathLabels": ["AI/互联网/IT", "电子商务"] }`
- 数字范围：`"age": { "min": 25, "max": 35 }`
- 薪资范围：`"expected_salary": { "min": "10万", "max": "30万" }`
- 自定义输入：`{ "label": "自定义", "input": { "min": "大专", "max": "本科" } }`

输入校验命令：

```bash
npm run validate:filter-input -- <platform> <input.json>
```

### 可复用搜索条件集

条件集是独立于岗位/JD 的平台隔离实体，权威记录位于
`data/<platform>/search-condition-sets/<conditionSetId>/`。每次创建、编辑、克隆或归档都形成不可变 revision；
`current.json` 只用于列出当前版本，`revisions/` 保留历史。引用始终包含 `conditionSetId`、`platform` 和
`revision`；不支持隐式“跟随最新版本”，也不支持跨平台翻译筛选字段。

创建/编辑时系统保存原始应用层筛选输入、编译后的 `SearchCondition[]` 快照和仅覆盖已选字段的 catalog 指纹。
运行时重新校验原始输入：无关目录变化且语义等价可继续执行；字段消失、值无效、路径歧义或编译条件变化则在
打开浏览器前失败。条件集可被归档但不会物理删除，已入队任务保留快照，后续新入队和调度轮次会被拒绝。

控制台提供条件集的创建、列表、revision 历史、克隆和归档；本地 CRUD 不打开浏览器也不调用模型。控制台和
HTTP 只接收业务输入和 ID，不能把任意服务端文件路径作为新的条件集来源。旧筛选 JSON 与已有岗位内嵌
`searchSettings` 保持兼容；只有含原始 `applicationFilterInput` 的岗位可被显式提升为条件集，只有编译条件的
历史记录不做反向猜测。

### 平台筛选要点

筛选字段和数量会随平台页面变化，完整可用值以 `data/<platform>/filter-catalog/application-filter-options.latest.json` 为准。本文只保留稳定的回放差异：

| 平台 | 稳定规则 |
| --- | --- |
| 51job | 应用前清理旧筛选和输入 tag；明确空结果按成功的 0 人处理 |
| Liepin | 薪资使用年薪“万”，年龄使用岁；行内文本和 checkbox 必须按真实输入/checked 状态验证；顶层 `keyword` 不重复作为筛选字段 |
| Zhilian | 年限、学历和年龄可使用预设或自定义范围，薪资为月薪；城市、户口、行业和职位等多级字段优先提交 `value` 与 `pathLabels` |
| Boss | 直接搜索和订阅管理均复用当前搜索页；直接搜索先读取完整目标差异，再按“职位 → 城市 → 其余条件 → 已查看策略 → 关键词”执行必要动作并整体核验，关键词在其他筛选稳定后只输入一次。所有完整搜索在最终条件核验后显式点击一次全局搜索按钮，字段已满足也必须点击；Enter 和中途搜索回退不作为提交，点击后需有新的结果周期证据才可提取候选。普通抓取的公共 `includeViewedCandidates` 仅在订阅搜索/直接搜索入口映射到“过滤近14天查看”，订阅管理保持由显式条件控制；与 direct 条件中的 `filter_recent_viewed` 冲突会在页面操作前失败，相同条件只执行一次。已满足字段不会重复操作；无法安全增量清除的残留筛选才会触发一次带原因记录的基线 reset。学历/经验自定义范围从 catalog 接收页面可见的语义边界，平台动作转换内部滑块索引后同时确认隐藏值与可见范围文案；重叠的“10年以上”手柄是有效精确状态。城市以一级选中集合为事实：选择“广东”即使用广东下二级城市的网页默认全部范围，不读取或点击肇庆等二级项；城市已匹配时不打开面板，最终整体核验只读取关闭页面的省级证据，不会再次确认面板。未选择的更多筛选不会用控件标题冒充结果。一次 direct 搜索共用一个按条件数量计算、上限 120 秒的 deadline；任一条件被重置、页面语义不符、已查看状态不符或结果刷新未被观察到都会在候选人提取前失败。已接入目录内单选、年龄/薪资范围、院校多选、学历/经验自定义范围、城市、职位范围、公司文本、专业与两个独立布尔筛选；当前实页没有“资格证书”，目录不得按固定槽位虚构字段。筛选回放和验证不调用 LLM。`verify:boss-filter-options` 默认只生成可审阅的 dry-run 计划，经单独授权的 `--run true` 会按生产节奏逐项回读状态并在 finally 恢复进入状态；动态建议及尚未纳入恢复合同的控件仍以现场 catalog 与验收报告为准 |

订阅管理应用筛选时，输出中的 `conditionResults` 会逐项记录 `applied`、`skipped` 或 `failed`，`allConditionsApplied` 汇总本次条件是否全部落页。只要存在 `skipped` 或 `failed`，显式保存订阅时会拒绝保存半成品条件。Boss 保存结果另外返回完整 `savedSearch` 引用和 `saveOutcome`；`renamed` 只有在改名前后仍是同一条件身份时才成立。名称、职位范围和页面关键词分别记录，排序策略不进入条件指纹。

订阅管理的平台范围有独立的显式扩展合同：plain `all` 只运行 `51job -> liepin -> zhilian`，`all + includeBoss=true` 才追加 Boss 第四阶段。Boss 阶段只选择/保存“我的订阅”，不会打开候选详情、写 seen、评分、报告、转发或邮件；任一阶段失败立即停止，已完成平台的外部保存不自动回滚。普通 Boss 订阅搜索则只接受完整引用，缺失或水合/最终提交失败时在候选读取前 fail closed。

普通简历抓取的直接搜索复用同一套 application options 和筛选输入格式，但它是正常抓取流程的一部分：应用筛选后会继续候选提取、详情打开、评分、导出和邮件。订阅管理仍然独立，只做条件落页、结果数读取和可选保存。

Boss 另有 `boss:apply-search-condition-set` 的 apply-only 本地操作入口，仅接受固定 Boss 条件集 revision、可选关键词和
`recent-viewed-policy`。它先校验条件集目录漂移与 viewed toggle 冲突，再使用一个按条件动作数估算、上限 120 秒的
搜索 deadline 应用和验证；普通 Boss 抓取也使用同一个 platform estimator，而非固定 30 秒。工作流持有 Boss 专属运行锁，
只在最终完整业务验证（完整激活职位、语义化范围、城市集合、toggle 和稳定结果）成功后输出 JSON。取消、失败或超时不输出
成功记录，并在原 deadline 剩余时间内尽力回到筛选基线。成功 JSON 包含实际变更的 `changedFields`、已满足而跳过的
`alreadySatisfiedFields`，以及必要 reset 的 `resetReason`。该入口不进入候选提取、详情、seen、评分、报告、转发或 LLM；
它不属于订阅管理或 `platform all`。

条件集 apply-only 同样属于一次完整 Boss 搜索：条件已落页且验证成功后必须点击一次最终搜索按钮，`resultTotal` 只能读取
该点击后的结果周期；准备页面和单字段维护动作不触发提交。

### 实站验证

Liepin 全部选项实站验证脚本默认只生成计划，不打开浏览器：

```bash
npm run verify:liepin-filter-options -- --keyword "优衣库" --limit 10
```

确认计划后再分批实站执行：

```bash
npm run verify:liepin-filter-options -- --keyword "优衣库" --run true --limit 10
```

常用参数：`--field work_years,education` 限定字段；`--offset`/`--limit` 分批；`--include-defaults false` 跳过“不限”；`--include-range-combinations true` 对薪资/年龄跑所有有效上下限组合；`--stop-on-failure true` 遇到首个失败即停。输出默认写入 `data/liepin/filter-catalog/option-verification/`，dry-run 是计划 JSON，实站运行是逐行 JSONL。

## 内部运营控制台与智能助手

控制台由 `npm run api` 启动本地 HTTP 服务，默认监听 `127.0.0.1:4180`，并从 `frontend/dist` 提供已构建的前端静态文件。一级工作区包括控制台、任务中心、岗位与人才、人才地图、Boss 工作台、自动化、知识与运营、智能助手和设置；新建任务通过独立路由进入。人才地图页面提供项目概览、公司矩阵、人才清单、运行记录和权威计划只读视图；Boss 工作台集中提供职位/JD、人才发现、会话中心、自动沟通审核和幂等操作回执。

生产模式先构建前端再启动 API。开发模式需要 API 与 Vite 分别在两个终端中同时运行；Vite 默认监听 `127.0.0.1:5173`，并把 `/api` 代理到 `127.0.0.1:4180`。

```bash
# 生产模式
npm run web:build
npm run api

# 开发模式终端 1
npm run api

# 开发模式终端 2
npm run web:dev
```

`npm run build` 已同时执行服务端 TypeScript 构建和 `web:build`。

可用环境变量：

- `AUTORECRUIT_CONSOLE_HOST`：控制台 API 监听地址，默认 `127.0.0.1`。
- `AUTORECRUIT_CONSOLE_PORT`：控制台 API 端口，默认 `4180`。
- `AUTORECRUIT_CONSOLE_API_KEY`：可选 bearer token；设置后所有 `/api/*` 请求都需要 `Authorization: Bearer <token>`。
- `AUTORECRUIT_CONSOLE_MAX_BODY_BYTES`：请求体上限，默认 2 MiB。
- `AUTORECRUIT_CONSOLE_FRONTEND_DIR`：静态前端目录，默认 `frontend/dist`。
- `VITE_API_BASE_URL`：前端 API 基址，默认 `/api`。

客户端把 API 地址写入 `localStorage`，把控制台 Bearer token 只写入当前浏览器会话的 `sessionStorage`。控制台 token 只进入 `Authorization` header；它与请求级模型 API key 是两套凭据，不能混用。生产客户端不会在 API 断开或请求失败时回退到 mock 候选人、任务或运行数据。

控制台后端使用单进程 `TaskQueue`，普通抓取、批量、Talent Mapping 浏览器运行/分类建议、订阅管理、登录刷新、Boss 人才发现/单人打招呼/原子会话操作/职位同步/自动沟通和 RAG 运营任务都会串行入队，任务记录落在 `data/runtime/tasks/`。计划任务组采用 prepare、任务文件持久化、immutable group manifest 提交、内存 publish 的顺序；manifest 是唯一 durable commit point，未匹配 manifest 的残留计划任务不会进入 pending 或执行。相同 group ID 只有在有序任务定义、计划元数据和失败策略的 canonical fingerprint 完全一致时才可幂等复用，启动恢复会重算指纹并隔离损坏或未提交的组。已保存职位、候选人、评分、运行结果、筛选 catalog 和 RAG 文件仍读取原有 `data/<platform>/...` 结构，Mapping read model 和质量审核服务则读取 `data/talent-mapping/...`。只有 `mappingStage=scan` 且 plan 为 `card-only` 的 Talent Mapping 可以调度；详情补全计划或阶段会在入队前拒绝，循环计划不能静默授权详情打开。

新建任务页面和自动化页面的搜索选择器在同一业务层直接展示五种模式：按岗位设置抓取、订阅搜索、直接搜索、批量抓取和订阅管理；分组、排序、名称和声明效果均来自 `GET /api/operation-modes?surface=manual|schedule` 的 `groups + modes` 响应。typed API client 先以 `unknown` 接收响应，再使用带 surface 上下文的运行时 parser 校验结构、全局唯一性、五种搜索映射、effects 双字段、每个 mode 的完整 surface/picker 集合和 picker 关系；页面只消费已验证目录，并直接复用共享搜索 mode ID 类型与守卫，不维护第二套目录。两个页面都以 discriminated union 保持搜索与独立任务只有一个活动分支，提交时再由共享编译器生成兼容的 task kind/source；计划创建成功时 selection 与平台表单一起重置。Talent Mapping、Boss 职位同步和登录刷新保持自动化页独立的类型化入口，不混入搜索目录；Boss 自动沟通仅保留在一次性新建任务和助手确认入口。normalizer 在任务摘要中记录 `modeId`、`modeLabel`、`declaredEffects` 和按本次输入解析的 `resolvedEffects`；这些字段只用于审计和展示，不授予额外执行权限。CLI 直跑的模式公告继续写 stderr，TaskQueue 通过显式 reporter 将同一公告记录为 info，不把成功任务标成错误。模式目录加载、请求或合同校验失败时，搜索/抓取创建 fail closed 并显示重试；独立任务、登录刷新和历史查看仍可用，不静默回退到第二套中文目录。新建任务页会保留条件集和旧筛选文件的本地状态，但仅在直接搜索、订阅管理或显式 direct 的 batch 默认来源下按目标平台白名单发送。

新建普通抓取和 batch 任务在 Boss 阶段可提交 `bossScreeningEnabled`、策略文件、主转发/主报告与否定邮件副收件人/CC 字段；batch 条目可覆盖主转发、主报告和否定邮件三组 CC，显式空数组清空而省略才复用。旧 `bossSecondaryForwardMode`、`bossSecondaryForwardRecipient`、`bossSecondaryForwardCc` 会在 normalizer 统一拒绝。服务端只接受 Boss 字段用于 `platform=boss`，或 `platform=all && includeBoss=true`；助手草稿只保留这些结构化字段和策略文件引用，不接受表达式、脚本或收件人规则。入队前会把单岗位设置或每个 batch 条目的完整交付/策略解析结果固化为 hash 保护的 settings v3/task v4 私有快照，客户端不能自行注入该字段。页面明确展示“需复核 → 主转发、明确否定 → 不转发并逐份邮件”的语义，并为三组 CC 提供显式清空开关；最终配置、完整性和队列执行仍以服务端为准。

### 循环自动运行

控制台“自动运行”页可以创建持久化计划，把 `resume-capture`、`batch`、`talent-mapping`、`search-subscription` 或 `boss-job-sync` 任务组成串行轮次。该集合由 `src/operation-modes.ts` 的共享运行时 guard 定义；operation-mode 的 `schedule` surface 不等于持久化循环授权，因此登录刷新等一次性自动化入口不会借此获得 recurring 资格。搜索计划首先从共享目录选择“按岗位设置抓取、订阅搜索、直接搜索、批量抓取、订阅管理”，独立计划另选 Talent Mapping 或 Boss 职位同步；用户界面把 `search-subscription` 称为“订阅管理”。`boss-auto-chat` 保留 CLI、普通 TaskQueue、一次性 HTTP/控制台任务和助手确认能力，但绝不属于循环计划；create/update 会以稳定 code `scheduled-task-kind-not-allowed` 拒绝它。普通抓取、batch 或订阅管理计划选择 `all` 时，可显式保存 `includeBoss: true`；历史计划缺少该字段一律按 `false` 处理。自动化订阅管理固定只读，只选择并读取原生“我的订阅”：schedule create/update、恢复和每轮执行共用的 schedulable normalizer 会拒绝 `saveSearchSubscription=true` 或订阅名称，因而不会保存或改名平台订阅；手工或助手明确确认的独立订阅管理仍可显式保存。旧保存型模板仍可读取，自动化详情会明确提示其将在恢复或运行前被拒绝。普通抓取/batch 的 Boss 阶段才可能打开详情并复用岗位转发。Boss 分流字段随普通抓取/batch 模板保存；每个调度轮次重新经同一 normalizer 解析岗位设置并生成该轮不可变的单岗位或 batch 快照，之后岗位或策略文件变更不影响已排队任务，UI 预览也不授予执行权限。Talent Mapping 调度固定为 card-only `scan`；`targeted-detail`、`full-detail`、`enrich` 和 `all` 都不可调度。Boss 人才匹配、打招呼、原子会话变更和自动沟通均不可调度。计划受指定时区和每日时间窗口约束；新一轮从上一轮全部任务完成后计算间隔，`delaySeconds` 为 `0` 时会立即尝试下一轮。计划任务与手工任务共享同一个 `TaskQueue`，不会并发控制浏览器。

调度器支持失败后间隔、整轮遇错停止或继续，以及连续失败达到阈值后暂停。计划和轮次分别持久化到 `data/runtime/schedules/` 与 `data/runtime/schedule-runs/`，服务重启后仍可恢复。恢复、启用、立即运行和每轮入队都会重新校验完整模板；历史 `boss-auto-chat`、unknown 或混合模板不会删除或静默改写为其他业务模式，而是暂停、清除 `nextRunAt` 并记录只含 task key、kind 和安全说明的 `validationIssues`。此类配置错误不创建轮次、不进入普通 failure-delay 重试；详情可审计但禁用启用/立即运行，只有用完整合法 tasks 更新后才会清除问题，且不会自动恢复启用。停止计划采用“当前任务结束后停止”：正在运行的单个任务完成后，取消本轮余下任务并停止后续轮次；`--platform all` 和一次 batch 各自都视为不可从内部强制中断的单个任务。

计划文件的 raw 持久化记录、合法可执行模型和 API 安全读取视图彼此分离。服务端 pure record inspector 把整个 JSON 当作 `unknown`，逐字段检查计划身份、名称、状态、时区、窗口、repeat、失败策略、计数、时间戳、run ID、任务和历史 issue；只有元数据与任务均合法时才生成 executable model。任务结构损坏使用 `scheduled-task-template-invalid`，计划标量或身份损坏使用 `schedule-record-invalid`；安全详情以固定 fallback 和不含原始 `input` 的占位任务展示，读时诊断不写回文件。详情响应以 `readViewVersion: 1` 标识合同版本，但版本号本身不是信任依据：typed API client 仍以 `unknown` 接收 current/legacy 响应，逐字段及逐 task item 校验，并在合同损坏时合成一个去敏诊断。raw 顶层扩展字段、对象型声明字段、内部 storage revision 和历史 issue 原文不会透传；历史 issue 的 token/message 会 canonicalize。quarantine 只在恢复、控制或轮次最终闸门中发生，保留 raw `tasks` 原值；任何结构、元数据或 kind 问题均 fail closed，暂停/停止入口仍可用于收敛计划。

仓库支持的计划写入统一使用每个 schedule 的跨实例/进程 lease 和单调 storage revision。`open(..., 'wx')` 是 lease 获取线性化点，随机 owner token 限制释放只能删除自身 generation；普通 writer 发现死 owner、损坏且超龄的 owner 时返回 `schedule-lease-recovery-required`，绝不根据旧观察自动删除固定 lock path。离线恢复要求先停止所有 API/scheduler 进程并显式确认，只把精确 schedule lock 原子移动到唯一 quarantine 路径；命令完成前不能重启 writer。状态转换持锁后重新读取当前 JSON，再判断 start、pause、stop、run-now、恢复、时间窗、失败计数、quarantine、active-run 收口或最终入队前置条件；旧快照只能得到明确冲突，不能覆盖较新的文件。GET/list/detail 不获取写 lease，也不刷新 revision 或 `updatedAt`。人工直接编辑 JSON 仍属于非原子维护，但受支持写入者会在持锁后看到已经落盘的人工变更。最终记录/模板复核与任务组入队位于同一 lease 内；任务组在所有 task files 和 manifest 成功后才视为 accepted，入队前的持久化失败不创建 run、不增加连续失败计数。accepted 后 run/schedule 落盘短暂失败时，当前进程只复用带明确 pending dispatch identity 与相同 fingerprint 的组，不认领任意“最新历史组”。进程重启仍按既有 fail-closed 语义把未完成轮次标为 interrupted，不承诺从外部动作中点继续。

控制台提供启动、暂停、立即运行和“当前任务结束后停止”操作；含阻断诊断的历史计划会保留暂停/停止收敛入口，但禁用启动和立即运行。命令行控制入口见 [`README.md`](./README.md#循环自动运行)。调度器必须复用任务 normalizer 和全局队列，详细代码约束见 [`src/server/AGENTS.md`](./src/server/AGENTS.md)。

### API 边界

API 按职责分组，具体路由清单以 [`src/server/routes.ts`](./src/server/routes.ts) 和 [`src/server/http.ts`](./src/server/http.ts) 为准：

| 路由组 | 代表入口 | 职责 |
| --- | --- | --- |
| 健康与仪表盘 | `/api/health`、`/api/dashboard/health` | 服务与依赖状态 |
| 操作模式目录 | `/api/operation-modes?surface=manual|schedule` | 公开业务模式、picker 分组/顺序和声明影响；不返回 argv、快照或平台 selector |
| 任务 | `/api/tasks/*` | 提交和读取共享队列任务 |
| 自动运行 | `/api/schedules/*` | 计划、轮次和运行控制 |
| 职位与候选人 | `/api/jobs/*` | 读取职位、运行和候选人数据 |
| Talent Mapping | `/api/tasks/talent-mapping`、`/api/tasks/talent-mapping-classification`、`/api/talent-mappings/*` | 浏览器/分类任务经队列执行；读取变化、实体关联和分类审核，本地人工确认接口不操作浏览器 |
| Boss 持久化读取 | `/api/boss/*` | 读取职位快照、同步记录、自动沟通审核和幂等回执 |
| 安全工件 | `/api/artifacts/:artifactId` | 下载服务端登记的报告、快照和回执 |
| RAG | `/api/rag/*` | 候选人问答和历史对话导入 |
| 运营 | `/api/ops/*` | 筛选 catalog、筛选输入和运营数据 |
| 智能助手 | `/api/assistant/*` | 草稿生成、校验和确认 |

Boss 新能力对应的队列入口是：

| 入口 | 默认风险级别 | 关键校验 |
| --- | --- | --- |
| `POST /api/tasks/boss-talent-search` | 只读；`triggerMatch=true` 时消耗配额 | 平台固定 Boss；立即匹配需要 `confirmed=true` |
| `POST /api/tasks/boss-greet` | 联系候选人 | 精确候选人 ID、预期姓名/职位和 `confirmed=true` |
| `POST /api/tasks/boss-chat-operation` | 按 action 区分只读与变更 | 变更需要 conversationId、intentId 和 `confirmed=true` |
| `POST /api/tasks/boss-job-sync` | 读取平台并更新本地 JD | 平台固定 Boss；可指定职位 ID；允许调度 |

Talent Mapping 浏览器运行使用 `POST /api/tasks/talent-mapping`。Shared normalizer 校验 plan 和相对 search plan、核心平台范围、stage/run ID 组合、禁止字段及当轮 `confirmedDetailOpen`，再构建权威 argv；`argvPreview` 不能成为执行来源。`completed-with-gaps` 仍是成功 TaskQueue 输出，但 output summary 保留状态、受限切片和详情失败数量。

质量接口包括 `GET /changes`、`GET/POST /entity-links`、`POST /entity-links/:entityId/revoke`、`GET /classification-suggestions`、`POST /classification-suggestions/generate` 和 `POST /classification-suggestions/:suggestionId/review`。分类生成也可直接提交 `POST /api/tasks/talent-mapping-classification`，normalizer 只接受 `mappingKey` 和不超过 100 的 `limit`，拒绝请求携带 API key 或任意模型设置；模型凭据和模型名只从运行环境读取。实体确认、撤销和分类审核更新本地审计事实后重建派生视图及最新导出，不触发浏览器动作。

这些路由只负责校验并入队，不直接调用 Playwright。HTTP 返回任务后，真实浏览器动作由串行 `TaskQueue` 执行。控制台助手确认同样复用这些 normalizer；风险确认不能代替请求体中的 `confirmed`、精确 ID 或 intent ID。

Boss 持久化 read model 的只读入口是：

```text
GET /api/boss/positions
GET /api/boss/job-sync/runs
GET /api/boss/job-sync/runs/:runId
GET /api/boss/chat-reviews
GET /api/boss/chat-reviews/:runId
GET /api/boss/chat-receipts
GET /api/boss/chat-receipts/:intentId
```

这些 GET 路由只读取本地持久化工件，不启动浏览器、不访问在线 Boss 页面，也不消耗匹配额度或触发候选人联系。人才发现和会话只读操作仍是队列任务；不能把任务内临时输出伪装成跨重启历史。

`TalentMappingReadModel` 提供以下本地只读入口：

```text
GET /api/talent-mappings
GET /api/talent-mappings/:mappingKey
GET /api/talent-mappings/:mappingKey/runs
GET /api/talent-mappings/:mappingKey/candidates
GET /api/talent-mappings/:mappingKey/companies
GET /api/talent-mappings/:mappingKey/coverage
```

这些 GET 路由不进入 `TaskQueue`、不打开浏览器，也不能触发扫描或详情补全。项目详情同时使用与工作流相同的纯选择模块计算最近成功扫描的精确详情候选数；前端安全对话框展示这个数量、平台分布和 source run ID，只有当轮勾选确认后才提交 `confirmedDetailOpen=true`。

`GET /api/artifacts/:artifactId` 只解析服务端生成的白名单引用，允许类型限于登记过的报告、文本/DOM 快照和回执。接口拒绝任意本地路径、路径分隔符、`..`、`.env`、浏览器 storage state、profile 和未登记附件。

控制台的 `智能助手` 页面对应三组 API：

- `POST /api/assistant/chat`：根据对话生成或更新结构化 `AssistantDraft`。
- `POST /api/assistant/validate`：重新清洗并校验 draft，返回缺失字段、警告和可确认预览。
- `POST /api/assistant/confirm`：确认 draft。除 `rag-answer` 外，都会复用 shared normalizer 后进入 `TaskQueue`；`rag-answer` 直接调用 RAG 问答并返回答案。

控制台的 `配置` 页面提供 `中转地址`、`模型名称` 和 `API Key` 三项请求级模型覆盖：

- 只随 `POST /api/assistant/chat` 和 `POST /api/rag/answer` 发送，用于本次助手草稿生成或控制台 RAG 问答。
- 不写入任务草稿、`TaskQueue`、任务记录、RAG answer log 或后端配置文件。
- 中转地址和模型名称保存在 `localStorage`，API Key 只保存在当前浏览器会话的 `sessionStorage`；留空时使用后端环境变量。
- 确认后的 JD 解析、评分、抓取、导出、邮件和 RAG 运维仍使用各自运行环境，不受控制台请求级配置影响。

当通用 `LLM_COMPLETION_ROUTE=codex-session` 时，请求级中转地址、模型名称和 API Key 覆盖全部拒绝，避免把同一次请求混入另一模型服务；切换路径只能通过启动环境变量完成。简历评分另由 `SCORING_LLM_COMPLETION_ROUTE` 控制，默认 `codex-session`，显式设为 `default` 才使用原 `SCORING_MODEL`/`OPENAI_*` 接口；该覆盖只用于普通简历评分和 Boss 组合评分/条件核验，不改变 JD、RAG、助手或 Mapping 路由，两条评分路径不自动回退。Codex 路径每个调用使用新的 ephemeral Codex 线程和临时只读工作目录，使用当前登录账户的默认或 `CODEX_SESSION_MODEL` 指定模型，但不复用控制台对话或业务任务状态；任何工具调用事件会中断该调用。`CODEX_SESSION_CONNECT_TIMEOUT_MS` 只分别约束 process/initialize/thread-start/turn-start 握手，收到 turn 接受或处理事件后不再设置固定总时限，只等待明确完成、协议/进程失败或人工终止。失败诊断只保存 provider、kind、phase、首个输出状态、耗时和时间，不保存 prompt 或模型正文。`npm run llm:route:doctor` 只报告路由配置，附加 `-- --verify true` 才会发送固定、无业务数据的连通性请求。

智能助手只负责把自然语言整理成受控草稿，不是命令执行器。模型输出会按任务类型过滤字段，任意 shell、npm script、文件写入或绕过 normalizer 的请求应被拒绝或作为风险提示；最终确认仍必须通过 `src/server/task-normalizers.ts` 的同一套校验。`argvPreview` 只是给用户核对的预览，不是执行来源。

助手支持的 draft 类型包括 `resume-capture`、`batch`、`talent-mapping`、`search-subscription`、`login-refresh`、`boss-auto-chat`、`boss-talent-search`、`boss-greet`、`boss-chat-operation`、`boss-job-sync`、`rag-ops` 和 `rag-answer`。Mapping 草稿只接受核心平台、计划文件、stage、source run ID 和详情确认字段；确认后仍重新加载 plan 并经过 shared normalizer。Boss 草稿和确认仍必须通过共享 normalizer，平台固定为 Boss；立即匹配、打招呼或会话变更必须明确展示风险并同时通过 draft 内的确认字段和最终风险确认。`rag-answer` 保持候选人问答语义：不会创建任务、不会打开浏览器、不会抓候选人、不会评分、不会导出报告或发送邮件；已保存职位和临时 JD 的索引/日志行为与 `--jd-question` / `--rag-question` 保持一致。

## 运行模式

### 单职位模式

首次运行一个新职位：

```bash
npm run dev -- --platform <51job|liepin|zhilian|boss> --keyword "<关键词>" --jd "<JD 文本>"
```

或从文件读取 JD：

```bash
npm run dev -- --platform <platform> --keyword "<关键词>" --jd-file ./fixtures/jd.txt
```

重跑已有职位时可以不再提供 JD：

```bash
npm run dev -- --platform <platform> --keyword "<关键词>"
```

根据已保存或临时 JD 回答候选人问题，不会打开浏览器、抓候选人、评分、导出或发邮件：

```bash
npm run dev -- --platform <platform> --keyword "<关键词>" --jd-question "这个岗位薪资范围是多少？"
npm run dev -- --platform <platform> --keyword "<关键词>" --jd-file ./fixtures/jd.txt --jd-question "这个岗位需要出差吗？"
```

`--rag-question` 是 `--jd-question` 的别名，两者遵循相同隔离规则：

- 已保存职位优先复用 `data/<platform>/jobs/<jobKey>/jd.json` 和持久化 RAG；缺少索引时自动索引当前 JD，不重新解析 JD。
- 找不到已保存 JD 时才读取本次 `--jd` 或 `--jd-file`，使用临时轻量检索回答，不创建职位记录、持久化索引或生产问答日志。
- 检索组合 Qdrant 向量、本地 BM25、融合排序和轻量重排。输出包含 `answer`、`answered`、`confidence`、`noAnswerReason` 和 `sources`。
- 没有可信 JD/已确认招聘方事实，或最高可信片段低于 `RAG_MIN_CONFIDENCE_SCORE` 时，直接返回明确的无答案结果，不调用模型编造。

RAG 运维入口按用途分组；完整参数和日常处理顺序见 [`docs/rag运营手册.md`](./docs/rag运营手册.md)，全部脚本名以 [`package.json`](./package.json) 为准：

```bash
npm run rag:index -- --platform <platform> --keyword "<关键词>"
npm run rag:ask -- --platform <platform> --keyword "<关键词>" --question "这个岗位需要出差吗？"
npm run rag:doctor -- --platform <platform> --keyword "<关键词>" [--question "这个岗位需要出差吗？"]
npm run rag:ingest-conversations -- --file ./conversations.jsonl [--dry-run true] [--fail-on-error false] [--doctor true]
npm run rag:ops -- --file ./rag-review-jobs.json [--question "这个岗位需要出差吗？"] [--policy ./rag-metrics-policy.json] [--output ./rag-ops.md] [--fail-on-issue true]
```

RAG 的稳定行为按能力分组如下：

| 能力 | 入口 | 稳定语义 |
| --- | --- | --- |
| 只读诊断 | `rag:inspect`、`rag:doctor`、`rag:doctor:batch` | 检查本地事实、索引、provider/model、Qdrant 和检索命中，不生成回答或改写索引 |
| 对话导入 | `rag:ingest-conversation`、`rag:ingest-conversations` | 按稳定 turn ID 增量、幂等合并；只把已确认的招聘方信息建成回答事实 |
| 人工审核 | `rag:feedback`、`rag:review`、`rag:review:batch` | 反馈写回职位 `answer-logs.jsonl`；批量审核只汇总指定职位 |
| 质量运营 | `rag:metrics`、`rag:ops` | 汇总健康、待审核和质量阈值；可用失败码接入 CI 或定时巡检 |
| 回归评测 | `rag:eval`、`rag:answer-eval`、`rag:regression` | 分别验证召回、最终回答和完整 suite；失败 case 返回非 0 |
| 内部 HTTP 接口 | `rag:api` | 复用 CLI 的职位隔离、事实信任、自动索引和日志语义；API key 只是轻量保护，不提供多租户或 RBAC |

默认基线使用本地 embedding HTTP 服务和 Qdrant；`npm run test:rag:offline` 则使用临时数据、内存向量库和确定性假 embedding，不调用 OpenAI 或连接 Qdrant。Fixture 必须脱敏，只有已确认招聘方事实可标记 `verified: true`，并应包含资料未说明或未确认的负例。

完整参数、质量 policy、模板文件、基线维护和本机服务排障见 [`docs/rag运营手册.md`](./docs/rag运营手册.md)；RAG 的事实信任、职位隔离和无答案约束见 [`src/rag/AGENTS.md`](./src/rag/AGENTS.md)。

普通抓取的“订阅搜索”对应内部 `--search-source saved`，会使用平台已保存的订阅/快捷搜索入口。需要临时主动输入筛选条件、不依赖保存标签时，使用“直接搜索”（内部 `direct`）：

```bash
npm run dev -- --platform <platform> --keyword "<关键词>" --jd-file ./fixtures/jd.txt --search-source direct --application-filter-input-file ./filter-input.json
```

direct 模式会打开平台搜索页、清理旧筛选、输入本次关键词、应用 `data/<platform>/filter-catalog/application-filter-options.latest.json` 支持的应用层筛选条件，然后继续同一套候选抽取、详情打开、评分、报告和邮件流程。`--application-filter-input-file` 只在 `--search-source direct` 下有效；筛选条件出现 `skipped` 或 `failed` 时会中止本次抓取，避免从半应用条件里抓候选人。

默认情况下，主流程会按平台默认状态设置已查看筛选。需要切换到包含已查看候选人的复跑时，显式打开 `--include-viewed`：

```bash
npm run dev -- --platform <platform> --keyword "<关键词>" --include-viewed true
```

平台行为：

- `51job`：默认勾选搜索页的 `我已看`；`--include-viewed true` 时取消它。
- `liepin`：默认勾选搜索页的 `隐藏已查看`；`--include-viewed true` 时取消它。
- `zhilian`：默认勾选可见的 `未看过`；`--include-viewed true` 时只取消可见的 `未看过`，不会改动 `未聊过`。设置后会确认快捷搜索条件仍显示 `关键词：<关键词>`，必要时重新选择快捷搜索标签再提取候选人。
- `boss`：默认勾选“过滤近14天查看”以排除平台近 14 天已查看人选；`--include-viewed true` 时取消勾选。无论页面状态如何，本地 `seen-ids.json` 都继续跳过已成功抓取的候选人。

Liepin 可在新增候选人进入详情页后自动转发给常用联系人。显式传入联系人姓名才会启用；转发成功后才继续解析和标记 seen，转发失败会让该候选人保持可重试：

```bash
npm run dev -- --platform liepin --keyword "<关键词>" --liepin-forward-contact "<常用联系人姓名>"
```

`--platform all` 的单职位运行也可以传入 `--include-viewed true` 和 `--liepin-forward-contact "<常用联系人姓名>"`。默认时，已查看页面筛选会应用到三个核心平台；若额外指定 `--include-boss true`，Boss 同样会按该开关设置“过滤近14天查看”，`--liepin-forward-contact` 仍只在 Liepin 阶段生效。`--boss-forward-mode` 与 `--boss-forward-recipient` 仅在 `all + includeBoss` 或单 Boss 时有效，并只在 Boss 阶段使用。

direct 单职位模式同样支持这些开关。Zhilian 仍会在新增候选人详情中复制 `转给同事` -> `链接转发` 分享链接；Liepin 仍只对新增候选人执行常用联系人转发。

### Talent Mapping 模式

Mapping 计划扫描示例：

```bash
npm run dev -- --platform all \
  --talent-mapping-file ./fixtures/talent-mapping/retail-operations.example.json \
  --mapping-stage scan
```

详情补全使用 `--mapping-stage enrich` 和当轮 `--mapping-confirm-detail-open true`，可通过 `--mapping-run-id` 精确引用成功 scan；`all` 则在同一运行内顺序执行两阶段。Mapping 参数不能与 `--jobs-file`、单岗位 keyword/JD、订阅管理、问答、邮件、include-viewed、Liepin/Boss 转发或任何 Boss 独立模式组合。Mapping 内部会包含已查看候选人以研究市场覆盖，但不暴露或复用普通抓取的公共 `--include-viewed` 开关。

控制台“新建任务”只负责提交安全的 scan；详情 enrich 从“人才地图”项目页发起，以便展示与执行工作流相同规则计算出的精确候选数量。HTTP 和助手也可以提交 Mapping，但都必须经过同一 normalizer。自动运行页只能创建 `card-only` plan 的 scan 任务，服务端会重新解析 plan 并拒绝任何详情能力；分类建议从人才地图的“分类审核”页进入独立队列任务。

### Boss 自动沟通模式

Boss 自动沟通只允许 `--platform boss`，且只能由 CLI、一次性任务或助手确认执行，不能创建、恢复、启用或立即运行循环计划。转发方式、收件目标和总结邮箱可在本次显式提供，也可复用岗位记录或 Boss 平台已保存配置；首次沟通候选人最终仍找不到转发配置时，会记录可重试失败。物业电工严格模式示例：

```bash
PLAYWRIGHT_HEADLESS=false npm run dev -- \
  --platform boss \
  --boss-auto-chat true \
  --boss-chat-require-all true \
  --boss-forward-mode email \
  --boss-forward-recipient "resume@example.com" \
  --boss-chat-summary-email "summary@example.com" \
  --boss-chat-summary-cc "audit@example.com"
```

参数说明：

- `--boss-auto-chat true`：进入独立未读会话审查模式。
- `--boss-chat-require-all true`：使用当前已配置岗位的全部硬性条件；物业电工会走六项明确证据判断。
- `--boss-chat-score-threshold 0..100`：不启用严格模式时使用的模型评分阈值，默认 70。
- `--boss-chat-reply-unqualified true`：可选；启用后才向不符合者发送固定拒绝话术，默认关闭。
- `--boss-forward-mode colleague|email`：符合者的转发方式。
- `--boss-forward-recipient`：站内同事姓名或简历收件邮箱。
- `--boss-chat-summary-email`：整批完成后的总结邮件收件人。
- `--boss-chat-summary-cc`：可选逗号分隔抄送列表，必须与总结收件人一起使用。
- `--boss-sync-jobs-before-review true`：可选；在读取未读会话前同步全部 Boss 职位/JD，任一同步项失败时中止本轮审查，默认关闭。

该模式不能和 `--keyword`、`--jobs-file`、`--jd`、`--jd-file`、普通 `--email`/`--cc`、`--include-viewed`、`--include-boss`、搜索来源、订阅管理或 JD/RAG 问答参数组合。未启用审查前同步时，应先确认岗位 JD 已保存在 Boss 平台目录；启用同步后，会话优先使用 Boss 职位 ID 解析岗位，只有缺少 ID 且名称唯一时才按名称回退。

总结邮件列出符合与不符合候选人的姓名、ID、理由和动作状态。若符合者已成功转发但后续消息或换电话失败，会明确显示“已转发，但联系动作未完成”。SMTP 发送失败不会删除已落盘的审查结果，但命令会失败，避免误报邮件已送达。

### Boss 人才发现与单人打招呼模式

推荐牛人和深度搜索是独立 Boss 模式，不创建普通抓取运行、不评分、不导出报告，也绝不会因普通 `all + includeBoss` 被触发。推荐模式只读取当前候选人卡片：

```bash
npm run dev -- --platform boss --boss-talent-source recommend
```

深度搜索可只读取当前职位、核心要求、加分项、剩余次数和候选人，也可传入 JSON 数组精确同步要求：

```bash
npm run dev -- \
  --platform boss \
  --boss-talent-source deep-search \
  --boss-job-id job-123 \
  --boss-expected-job-name "物业电工" \
  --boss-core-requirements-json '["持高低压电工证","2年以上物业经验"]' \
  --boss-bonus-requirements-json '["上海本地经验"]'
```

默认不会点击“立即匹配”。只有同时设置 `--boss-trigger-match true --boss-confirmed true`，且页面仍有剩余次数、按钮可用、至少存在一项核心要求时才执行匹配。结果最多保留最新 20 位候选人。

单人打招呼只支持推荐或深度搜索结果，执行前按精确候选人 ID 重新定位，并复核姓名、职位和可用的 Boss 职位 ID：

```bash
npm run dev -- \
  --platform boss \
  --boss-greet-source deep-search \
  --boss-greet-candidate-id candidate-123 \
  --boss-expected-candidate-name "候选人甲" \
  --boss-expected-job-name "物业电工" \
  --boss-job-id job-123 \
  --boss-confirmed true
```

候选人卡片已经显示“继续沟通”时返回已联系结果，不重复打招呼。候选人姓名或可见列表序号从不作为外部动作的主标识。

### Boss 原子会话操作模式

原子会话模式把一次会话读取或变更封装为一个独立操作。只读动作包括列出会话、打开会话、读取可见消息、向上读取历史和预览在线简历：

```bash
npm run dev -- \
  --platform boss \
  --boss-chat-operation list-conversations \
  --boss-unread-only true
```

除 `list-conversations` 外都必须提供精确 `--boss-conversation-id`；可同时提供预期候选人姓名和职位名，用于打开后复核。发送文本、设置备注、标记不合适、索要/接收附件简历、交换电话或微信属于变更操作，必须同时提供唯一 intent ID 和显式确认：

```bash
npm run dev -- \
  --platform boss \
  --boss-chat-operation send-text \
  --boss-conversation-id conversation-123 \
  --boss-expected-candidate-name "候选人甲" \
  --boss-expected-job-name "物业电工" \
  --boss-chat-text "方便沟通一下吗？" \
  --boss-intent-id contact-conversation-123-v1 \
  --boss-confirmed true
```

通用发送发现聊天框已有草稿时立即失败，不覆盖用户文本。成功变更按 intent ID 写入回执；同一 intent 重试直接返回原结果，若 intent 已用于其他动作或会话则拒绝执行。原子会话变更不能加入自动运行计划。

### Boss 职位/JD 同步模式

职位同步读取 Boss 职位管理中的开放、审核中和已关闭职位，按“职位名 + Boss 职位 ID”生成稳定岗位目录。默认包含已关闭职位，也可只同步指定 ID：

职位页面动作兼容旧版详情和当前内嵌的职位列表、编辑表单。当前页面会等列表行具备稳定职位 ID 后再读取，通过精确职位的编辑入口只读 JD，待表单内容完成加载后复核职位 ID 与名称并返回列表；整个过程不会触发职位保存。

```bash
npm run dev -- \
  --platform boss \
  --boss-job-sync true \
  --boss-job-ids job-123,job-456 \
  --boss-include-closed-jobs true
```

每个详情先读取 JD 并计算 SHA-256。Boss 职位同步固定使用页面字段和确定性规则，不调用 JD 模型；哈希与规则版本均未变化时不重写岗位记录。发生变化或规则版本升级时，必须先完成规则解析，才保留原创建时间、转发、报告和搜索配置并更新 JD。详情读取或规则解析失败只写入本次同步失败项，不覆盖上一份有效 JD。该模式是新增 Boss 模式中唯一允许调度的任务。

### 全平台模式

普通抓取和 batch 中，`--platform all` 默认按固定顺序串行运行：

1. `51job`
2. `liepin`
3. `zhilian`

明确增加 `--include-boss true` 后，Boss 直聘·直猎邦 Pro 在末尾作为第 4 阶段运行：

4. `boss`

`includeBoss` 只允许用于普通抓取、batch 或订阅管理的 `platform=all`，不能扩大筛选发现、JD/RAG 问答或 Talent Mapping 的平台范围。订阅管理的 Boss 阶段只操作原生“我的订阅”，不进入候选详情或普通抓取链路。每个平台使用独立浏览器会话和平台隔离存储。任一平台失败时，整体运行停止并抛出该平台错误；已完成平台的本地结果不回滚。

示例：

```bash
npm run dev -- --platform all --keyword "<关键词>" --jd "<JD 文本>"
```

将 Boss 加入本轮：

```bash
npm run dev -- --platform all --include-boss true --keyword "<关键词>" --jd "<JD 文本>"
```

全平台普通简历抓取支持 `--include-viewed true` 和 `--liepin-forward-contact "<常用联系人姓名>"`；后者只在 Liepin 阶段使用。启用 Boss 时，也可显式传入 `--boss-forward-mode` 与 `--boss-forward-recipient`，它们只在 Boss 阶段生效；省略时该 Boss 岗位可能复用已保存的转发配置。启用 Boss 不会触发推荐、深度搜索、立即匹配、打招呼、聊天或职位同步。

全平台也支持 direct 搜索来源：

```bash
npm run dev -- --platform all --keyword "<关键词>" --jd-file ./fixtures/jd.txt --search-source direct --application-filter-input-file ./filter-input.json
```

若要将 Boss 加入 direct 搜索，同样追加 `--include-boss true`。所有选中平台会用自己的 application options catalog 解释同一个筛选输入文件；输入文件必须只包含每个平台都支持的字段。系统在任何浏览器动作前完成 JD 与 catalog/input 预检，汇总全部失败后一次拒绝。预检通过后按 `51job -> liepin -> zhilian`（可选再接 `-> boss`）串行运行；任一平台筛选应用、候选抽取、转发、评分、导出或邮件失败时，`--platform all` 立即停止并抛出该平台错误。

### 批量模式

`--jobs-file` 接收 JSON 数组，每个条目至少包含 `keyword`，并可选包含 `jd`、`jdFile`、`email`、`cc`、`searchSource`、`applicationFilterInputFile`。

```json
[
  {
    "keyword": "东南亚 销售",
    "jdFile": "./fixtures/jd.txt",
    "email": "user@example.com",
    "cc": ["manager@example.com"],
    "searchSource": "direct",
    "applicationFilterInputFile": "./filter-input.json"
  }
]
```

运行：

```bash
npm run dev -- --platform <platform|all> --jobs-file ./jobs.json
```

批量模式不能与单职位参数 `--keyword`、`--jd`、`--jd-file` 混用。与 `--platform all` 组合时，外层顺序是 jobs 文件顺序，内层默认是 `51job -> liepin -> zhilian`；指定 `--include-boss true` 后，每个职位末尾追加 `-> boss`。

批量模式同样支持在 CLI 上传入 `--include-viewed true`，该开关会应用到本次 jobs 文件内的所有职位和平台；启用 Boss 时也会取消其“过滤近14天查看”。批量普通简历抓取也支持 `--liepin-forward-contact "<常用联系人姓名>"`，但只允许与 `--platform liepin` 或 `--platform all` 组合；`--platform all --jobs-file` 时只在每个职位的 Liepin 阶段转发，51job、Zhilian 和 Boss 不使用该开关。`--boss-forward-mode` 和 `--boss-forward-recipient` 则要求 `--platform all --include-boss true`（或单 Boss），并只在每个职位的 Boss 阶段使用。

批量模式可在 CLI 上传入 `--search-source direct --application-filter-input-file ./filter-input.json` 作为所有职位的默认 direct 筛选输入；jobs 文件内的 `searchSource` 和 `applicationFilterInputFile` 会覆盖 CLI 默认值。jobs 文件里的相对 `applicationFilterInputFile` 路径按 jobs 文件所在目录解析。

### 订阅管理模式

`--search-subscription-file` 是独立模式，只负责打开平台搜索条件页、填入关键词、应用支持的搜索条件、读取结果数，并在显式要求时保存搜索条件/订阅。它默认只运行 51job、Liepin、Zhilian；只有 `--platform all --include-boss true` 才追加 Boss。该模式不解析 JD、不创建职位记录、不抓取候选、不评分、不导出报告、不发送邮件，也不改变 seen 状态。

输入文件示例：

```json
{
  "keyword": "东南亚 销售",
  "savedSearchName": "东南亚 销售订阅",
  "conditions": []
}
```

运行：

```bash
npm run dev -- --platform 51job --search-subscription-file ./search-subscription.json --save-search-subscription true
```

全平台包含 Boss 的订阅任务：

```bash
npm run dev -- \
  --platform all \
  --include-boss true \
  --search-subscription-file ./search-subscription.json \
  --save-search-subscription true \
  --search-subscription-name "铝镁合金"
```

Boss 原生订阅的名称、职位范围和页面关键词是独立字段；例如名称可以是“铝镁合金”，职位范围仍是“全铝箱包设计”，页面关键词可以是“铝镁合金 拉杆箱”。订阅区域晚于搜索 iframe 异步挂载，动作会等待该区域就绪后再判断卡片或合法空列表；卡片缺少公开身份属性时，只在 Boss 页面动作内读取平台组件提供的原生订阅 ID、稳定职位证据和 keyed 条件标签，原始组件状态不会进入共享层或持久化。任务摘要保留 `nativeId`、完整条件身份、条件指纹、`saveOutcome` 和运行时 `sortPolicy`，不会把排序或本轮 viewed 覆盖写入保存条件指纹。普通订阅搜索若要复用该订阅，必须提供保存结果产生的完整 `bossSavedSearchReference`，不能只传订阅名称。

单岗位 HTTP/CLI 和 batch jobs-file item 都可携带该完整引用。显式把岗位切换为 direct 时会清除与 source 不兼容的 saved reference；排队任务不会把旧引用混入 direct 执行。全平台订阅管理遇错仍立即停止，但失败任务保留已经完成的平台摘要和 `stoppedPlatform`，使前序已经发生的保存/改名可审计且不会被误报为未执行。

已有岗位缺少该引用时，可用 Boss-only 绑定模式按完整引用重新验证原生卡片，再以 JobStore CAS 写回岗位；绑定过程不读取候选、不打开详情、不写 seen/评分/报告，必须显式提供 `--boss-confirmed true`。绑定失败或岗位 revision 已变化时不写回。

各平台可通过 `applicationFilterInputFile` 引用本平台生成的应用层筛选输入文件。字段不在当前平台 catalog、无法落页或无法确认时，必须在 `conditionResults` 中标记失败，不得静默忽略。

```json
{
  "keyword": "优衣库",
  "savedSearchName": "优衣库筛选输入结果数检查",
  "applicationFilterInputFile": "./51job-filter-input.example.json",
  "conditions": []
}
```

显式关闭复用浏览器运行示例：

```bash
PLAYWRIGHT_51JOB_REUSE_BROWSER=false npm run dev -- --platform 51job --search-subscription-file fixtures/51job-search-subscription-with-filter-input.example.json
```

Liepin 输入示例：

```json
{
  "keyword": "优衣库",
  "applicationFilterInput": {
    "work_years": "3-5年",
    "education": "本科",
    "school_nature": "211",
    "language": "英语",
    "expected_location": "上海",
    "age": { "min": 25, "max": 35 },
    "engaged_industry": {
      "value": "电子商务",
      "pathLabels": ["AI/互联网/IT", "电子商务"]
    },
    "engaged_function": "店长/卖场管理",
    "expected_industry": {
      "value": "电子商务",
      "pathLabels": ["AI/互联网/IT", "电子商务"]
    },
    "expected_function": "店长/卖场管理",
    "company_name": "迅销",
    "school_name": "辽宁大学",
    "major": "新闻学",
    "expected_salary": { "min": "10万", "max": "30万" },
    "current_salary": { "min": "10万", "max": "30万" },
    "overseas_work_experience": "有海外工作经验",
    "management_experience": "有管理经验"
  },
  "conditions": []
}
```

Liepin 可采集的 application options 多于当前已验证的页面回放字段；可用字段以 `data/liepin/filter-catalog/application-filter-options.latest.json` 和 [`src/platforms/AGENTS.md`](./src/platforms/AGENTS.md#filter-discovery-and-replay) 为准，不在本文复制易变化的完整清单。

回放时仍需遵守这些稳定规则：

- `age` 使用以岁为单位的数字范围对象。
- 当前/期望行业和职位使用页面对应字段；同名行业叶子应提供 `value` 与 `pathLabels` 指明完整点击路径，父级路径节点不能直接提交。
- `company_name`、`school_name` 和 `major` 是自由文本输入。
- `keyword_title` 由订阅管理顶层 `keyword` 控制，不重复作为 `applicationFilter`。

Zhilian 输入示例：

```json
{
  "keyword": "优衣库",
  "applicationFilterInput": {
    "education": {
      "label": "自定义",
      "input": { "min": "大专", "max": "本科" }
    },
    "work_years": {
      "label": "自定义",
      "input": { "min": "1年", "max": "3年" }
    },
    "school_nature": "985",
    "recent_activity_time": "近1周",
    "gender": "女",
    "job_status": "在职-看看机会",
    "talent_type": "金领人才",
    "talent_photo": "有照片",
    "resume_language": "中文简历",
    "job_hopping_count": "最近一份工作在职时长>1年",
    "language": "英语",
    "living_location": { "value": "上海", "pathLabels": ["热门", "上海"] },
    "hukou_location": { "value": "上海", "pathLabels": ["热门", "上海"] },
    "engaged_industry": {
      "value": "鞋服箱包批发/零售/贸易",
      "pathLabels": ["批发/零售/贸易", "鞋服箱包批发/零售/贸易"]
    },
    "expected_industry": {
      "value": "鞋服箱包批发/零售/贸易",
      "pathLabels": ["批发/零售/贸易", "鞋服箱包批发/零售/贸易"]
    },
    "engaged_function": {
      "value": "门店店长",
      "pathLabels": ["销售", "服务业销售", "门店店长"]
    },
    "expected_function": {
      "value": "门店店长",
      "pathLabels": ["销售", "服务业销售", "门店店长"]
    },
    "age": { "min": 24, "max": 31 },
    "expected_salary": { "min": "2千", "max": "1万" }
  },
  "conditions": []
}
```

订阅管理模式不能与 `--include-viewed`、`--liepin-forward-contact`、`--search-source` 或 `--application-filter-input-file` 组合。输出中的 `conditionResults` 会逐项记录 `applied`、`skipped` 或 `failed`，并通过 `allConditionsApplied` 汇总本次条件是否全部落页；Boss 的保存成功还必须返回可重新核验的完整引用和 `saved|already-saved|renamed` 结果。

## 运行要求

### 必需条件

- Node.js 24 LTS 或 Node.js 26。仓库 `.nvmrc` 固定为 `24`，`package.json` 声明 `>=24 <27`。
- 已安装依赖：`npm install`
- 已选择并配置一种文本模型路径：默认路径需要 `OPENAI_API_KEY`；`codex-session` 路径需要本机已登录的 Codex/ChatGPT。
- 可复用的平台登录态：通过 `npm run login:session -- --platform <platform>` 生成或刷新。
- 使用内部控制台前，先运行 `npm run web:build` 生成 `frontend/dist`，再启动 `npm run api`；开发前端时需要同时运行 `npm run api` 和 `npm run web:dev`。

### 可选配置

完整变量、示例值和注释以 [`.env.example`](./.env.example) 为准，运行时默认值以 [`src/config.ts`](./src/config.ts) 为准。本文只保留影响架构或运行边界的配置组：

| 配置组 | 主要变量 | 说明 |
| --- | --- | --- |
| 数据目录 | `DATA_DIR` | 默认使用 `data/`，所有平台职位数据继续按平台隔离 |
| 模型路由 | `LLM_COMPLETION_ROUTE`、`SCORING_LLM_COMPLETION_ROUTE`、`OPENAI_BASE_URL`、`OPENAI_MODEL`、`CODEX_SESSION_MODEL`、`CODEX_SESSION_CONNECT_TIMEOUT_MS`、`JD_PARSING_MODEL`、`SCORING_MODEL`、`TALENT_MAPPING_MODEL`、`RAG_MODEL` | 通用路由默认 `default`；评分专属路由默认 `codex-session`，可显式切回 `default`。`default` 只使用 OpenAI 兼容服务；`codex-session` 只使用当前本机 Codex/ChatGPT 登录创建的隔离临时线程。两者不自动互相回退；Codex 路径禁止工具、网络、MCP、网页搜索和文件变更。连接超时只约束逐阶段握手，模型 turn 没有固定总超时。|
| RAG 检索 | `RAG_EMBEDDING_*`、`RAG_*_TOP_K`、`RAG_RETRIEVAL_MODE`、`RAG_MIN_CONFIDENCE_SCORE` | 默认使用本地 embedding 与 hybrid 检索；低于可信阈值时明确拒答 |
| 向量索引 | `QDRANT_URL`、`QDRANT_API_KEY`、`RAG_VECTOR_COLLECTION` | Qdrant 是可重建索引，不是唯一事实源 |
| 控制台 | `AUTORECRUIT_CONSOLE_*`、`VITE_API_BASE_URL` | 控制监听、轻量 API 保护、请求体和前端目录；不构成完整认证网关 |
| 简历增强提取 | `.venv/bin/python` 与 `crawl4ai` | 不可用时自动回退到内置解析 |
| 邮件 | `SMTP_HOST`、`SMTP_PORT`、`SMTP_USER`、`SMTP_PASS`、`SMTP_FROM` | 用于报告和 Boss 审查总结交付 |

### 浏览器配置

- `BROWSER_ENGINE=cloakbrowser|playwright`：浏览器启动引擎，默认 `cloakbrowser`。如需回退到 Playwright 自带 Chromium，设置为 `playwright`。
- `PLAYWRIGHT_HEADLESS=true`：启用 headless。默认是 headed；Liepin 始终强制使用有头浏览器，此变量对 Liepin 不生效。
- `PLAYWRIGHT_SEARCH_PAGE_TIMEOUT_MS`：普通搜索入口到候选列表 ready 的基础总等待预算，默认 `30000`。51job 和 Boss 会在编排开始前按平台动作节奏、筛选条件和稳定结果核验计算更大的有界单 deadline（最高 `120000`）；这保留入口到候选快照的同一绝对截止时间，不会为后续列表读取重置预算。
- `PLAYWRIGHT_EMPTY_RESULTS_STABLE_MS`：51job 空列表稳定窗口，默认 `2000`。
- `PLAYWRIGHT_API_FALLBACK_TIMEOUT_MS`：Liepin/Zhilian API fallback 短等待上限，默认 `3000`。
- `PLAYWRIGHT_RESUME_DETAIL_TIMEOUT_MS`：简历详情打开/就绪总等待预算，默认 `20000`。
- `PLAYWRIGHT_{ACTION|CANDIDATE}_DELAY_{MIN|MAX}_MS`：全局操作和候选人等待覆盖。
- `PLAYWRIGHT_<PLATFORM>_{ACTION|CANDIDATE}_DELAY_{MIN|MAX}_MS`：平台级覆盖；默认值和加权规则统一见[浏览器操作节奏](#浏览器操作节奏)。
- `PLAYWRIGHT_MOUSE_SPEED_MIN_PX_PER_SECOND` / `PLAYWRIGHT_MOUSE_SPEED_MAX_PX_PER_SECOND`：全平台共享指针的真人移动速度范围，默认 `700` / `1200 CSS px/s`。
- `PLAYWRIGHT_BOSS_TYPING_DELAY_MIN_MS` / `PLAYWRIGHT_BOSS_TYPING_DELAY_MAX_MS`：Boss 搜索关键词、直接聊天文本和备注的逐字间隔，默认 `80` / `180`；常用语不受影响。
- `PLAYWRIGHT_REUSE_BROWSER=true|false`：全平台可复用浏览器默认开关。默认不启用；51job、Liepin、Zhilian 和 Boss 有平台默认值，会默认启用。
- `PLAYWRIGHT_51JOB_REUSE_BROWSER=false` / `PLAYWRIGHT_ZHILIAN_REUSE_BROWSER=false` / `PLAYWRIGHT_BOSS_REUSE_BROWSER=false`：关闭对应平台可复用浏览器。
- `PLAYWRIGHT_LIEPIN_REUSE_BROWSER=false`：关闭 Liepin 可复用浏览器；默认开启。开启时流程结束会关闭最后的详情页、停留在人才搜索页并保留浏览器，下次 Liepin 运行会优先连接这个窗口。
- `PLAYWRIGHT_51JOB_REUSE_CDP_PORT` / `PLAYWRIGHT_LIEPIN_REUSE_CDP_PORT` / `PLAYWRIGHT_ZHILIAN_REUSE_CDP_PORT` / `PLAYWRIGHT_BOSS_REUSE_CDP_PORT`：各平台可复用浏览器的本地 CDP 端口，默认分别是 `19325`、`19327`、`19329`、`19331`。

CloakBrowser 自带诊断和维护 CLI：

```bash
npx cloakbrowser info        # 查看 CloakBrowser 版本、平台、二进制路径和安装状态
npx cloakbrowser install     # 预下载 CloakBrowser Chromium
npx cloakbrowser update      # 检查并下载新版 Chromium
npx cloakbrowser clear-cache # 清除 CloakBrowser 浏览器缓存
```

正常多平台运行时应保持 `STORAGE_STATE_PATH` 未设置，让系统按平台选择默认 storage state。显式设置时，路径必须与当前平台匹配，避免把一个平台的登录态套用到另一个平台。

## 运维入口

安装、登录、普通抓取、Boss 操作和控制台启动的可复制命令见 [`README.md`](./README.md)；所有脚本以 [`package.json`](./package.json) 的 `scripts` 字段为准。本文仅保留验证和离线维护入口，避免与运行模式章节重复。

安装和验证：

```bash
npm install
npm run typecheck
npm run test
npm run build
npm run web:typecheck
npm run test:web
npm run test:talent-mapping
npm run web:build
```

聚合脚本已覆盖客户端：`typecheck` 包含 `web:typecheck`，`test` 包含 `test:web`，`build` 同时构建服务端与前端。

离线维护：

```bash
npm run reparse:resumes -- <platform> <jobKey>
npm run score:stored -- <platform> <jobKey>
npm run export:results -- <platform> <jobKey>
npm run email:report -- <platform> <jobKey> <recipient> [cc1,cc2]
npm run smtp:diagnose -- [--attempts 1-5]
npm run export:resume-docx -- --platform <platform> <jobKey> <candidateId> [--template /path/to/template.docx] [--output /path/to/resume.docx]
npm run export:resume-docx -- --resume-file ./resume.json [--snapshot-file ./snapshot.txt] [--template /path/to/template.docx] [--output /path/to/resume.docx]
npm run migrate:platform-storage
npm run validate:resumes
npm run capture:resume-dom -- --platform <platform> <jobKey> <searchKeyword> <candidateId>
npm run capture:resume-dom -- --platform <platform> <jobKey> <candidateId> <resumeUrl>
```

`export:resume-docx` 默认使用 `/Users/Admin/Downloads/简历模板.docx`。按平台、职位和候选人 ID 导出时，默认写入 `data/<platform>/jobs/<jobKey>/exports/resumes/<candidateId>-<name>.docx`；也可以通过 `--output` 或 `--output-dir` 指定输出位置。导出会尝试从已保存的详情页结构化内容中识别候选人本人头像并嵌入模板头像位；默认头像、学校图、页面图标、SVG 资源和相似人才头像都会被排除。如果无法确认或下载本人头像，则保留无头像导出，不放入模板示例头像或他人头像。

平台专项回归命令名保留 `experimental` 前缀以兼容已有脚本：

```bash
npm run test:experimental:liepin
npm run test:experimental:zhilian
```

## 架构说明

项目分为以下层次：

| 层次 | 职责 | 主要位置 |
| --- | --- | --- |
| 入口编排层 | 参数解析、saved/direct 普通抓取、批量、全平台循环、Talent Mapping、Boss 独立模式和结果收口 | `src/index.ts` |
| Talent Mapping 领域层 | Plan 校验、确定性归一化/选择、两阶段工作流、JSONL 事实、派生视图和 CSV/Markdown 导出 | `src/talent-mapping/`、`src/types/talent-mapping.ts` |
| 平台适配与工作流层 | 四个平台 adapter facade，以及 Boss 确认、身份、intent/回执、持久化和固定流程编排 | `src/platforms/*-adapter.ts`、`src/platforms/boss-*.ts` |
| 平台页面动作层 | 对 adapter 提供导航、搜索、筛选、候选、简历、转发/交付等类型化语义入口；实现层仍有上述已知收口项 | `src/platforms/<platform>/actions/` |
| 平台页面解析层 | 将平台详情文本或载荷转换为共享 `CandidateResume`，不执行页面变更；51job 启发式仍由共享浏览器 parser 持有 | `src/platforms/<platform>/parsing/`、`src/browser/resume-detail.ts` |
| 浏览器交互层 | Playwright 会话、节奏、无 selector 动作 context，以及尚待收回平台层的 51job 搜索页、列表和详情 helper | `src/browser/*.ts` |
| JD 解析层 | 非 Boss 输入的模型化 JD 结构化、schema 校验和清洗；Boss 职位同步的页面字段与确定性规则解析 | `src/parsers/jd-parser.ts`、`src/platforms/boss/parsing/job-parser.ts` |
| JD 问答层 | 从 JD 原文和结构化 JD 检索相关片段并回答候选人问题 | `src/rag/` |
| 简历提取层 | 页面文本/DOM 快照解析和可选 Crawl4AI 适配 | `src/browser/resume-detail.ts`、`src/extraction/` |
| 评分层 | 构造评分提示词、调用模型、校验评分 JSON | `src/scoring/` |
| 持久化层 | JSON、文本快照、DOM 快照、结果和导出文件 | `src/storage/job-store.ts` |
| 报告分发层 | 结果聚合、Markdown 渲染、DOCX 简历渲染、SMTP 邮件 | `src/reporting/`、`src/scripts/send-job-report-email.ts` |
| 控制台服务层 | HTTP API、任务队列、持久化调度器、职位/Boss/Mapping/artifact 读模型、RAG/筛选接口、智能助手确认执行 | `src/server/` |
| 控制台前端层 | 控制台、任务中心、岗位与人才、人才地图、Boss 工作台、自动化、知识与运营、智能助手、设置和新建任务 | `frontend/src/` |

### 语义动作模块设计标准

按业务语义组织页面动作是本项目所有招聘平台浏览器自动化的长期设计标准，也适用于以后新增的外部交互式系统集成。它解决的是页面操作的所有权、可复用性和安全边界，不要求“一个 DOM 控件一个文件”。一个动作单元应表达完整业务意图，例如“选择指定保存搜索”“应用并确认一个筛选条件”“按稳定 ID 打开候选人”“把简历转发给唯一同事”或“向精确会话发送已确认消息”；点击按钮、填写任意 selector 或执行任意 DOM 脚本不能作为公共动作。同一平台的动作可以被 CLI、HTTP、调度和以后新增的授权工作流组合，但复用动作不改变确认、身份复核、`TaskQueue` 和外部变更风险规则；跨平台复用的是动作合同和无 selector 基础，不是 selector 或页面实现。

标准依赖方向如下：

```text
CLI / HTTP / scheduler / workflow
                │  选择顺序、确认、身份、持久化
                ▼
       platform adapter / facade
                │  显式依赖所需领域动作
                ▼
     platform semantic domain action
                │  selector、pacing、deadline、前后置验证
                ▼
 selector-free shared browser primitives

page snapshot / payload ──> pure parser ──> typed domain result
```

必须遵守以下设计规则：

1. 动作以业务结果命名并接收类型化业务输入。稳定候选人、会话、职位或筛选身份应显式传入；不得让调用方传任意 selector、locator、点击回调或 DOM 脚本。
2. 一个动作完整负责页面前置状态、目标唯一性和身份复核、平台节奏、连续指针、兼容路径、共享 deadline 消耗以及业务后置状态验证。页面切换不能重置 deadline，已经满足的幂等状态必须显式返回。
3. adapter/facade 只注册动作和保留必要兼容签名；workflow 决定调用原因与顺序，处理模式隔离、风险确认、队列、回执和持久化；两者都不能绕过 action 直接操作页面控件。
4. 页面读取、页面变更和纯解析分离。read action 不依赖 mutation/forwarding/delivery action；parser 只转换快照或载荷，不依赖 Playwright、不写文件、不入队列、不调用模型。
5. selector 和兼容点击只能有一个平台领域所有者。共享浏览器 helper 必须无 selector、无平台条件；只有至少两个平台证明输入、输出和失败语义一致后，能力才能提升到共享层。
6. 私有兼容 runtime 只能容纳难以拆开的底层 fallback，不能继续持有导航、搜索、筛选、候选、简历和转发等多个公共领域的正常实现。只重导出大型 runtime 的领域文件不算完成模块化。
7. 每个新增或修改的页面动作都要有直接动作测试，并与架构边界测试、平台行为回归共同进入正式测试入口。测试至少按适用性覆盖成功、目标不存在或不唯一、身份变化、幂等状态、deadline 耗尽、后置状态未生效以及兼容点击的连续指针行为。

该标准的规范性约束以根 `AGENTS.md` 和 `src/platforms/AGENTS.md` 为准，本文记录设计理由和当前架构。前文列出的 Liepin/Zhilian 私有多领域 runtime、51job 浏览器层页面 helper 以及 Boss 普通搜索域集中问题属于现存迁移债务；新功能不得扩大这些债务，只有实现所有权和直接动作测试全部收口后才能声明全平台符合本标准。

核心源码位置：

- `src/index.ts`：CLI 主入口和总编排，包括单职位、全平台、批量、订阅搜索/直接搜索普通抓取、Talent Mapping、订阅管理和全部 Boss 独立模式。
- `src/config.ts`：环境变量读取与运行时配置。
- `src/types/job.ts`：JD、候选人、简历、评分、运行结果等共享类型。
- `src/types/boss.ts`：Boss 人才搜索、打招呼、原子会话操作和职位同步输入/输出类型。
- `src/types/talent-mapping.ts`：Mapping plan、卡片/详情 observation、run、覆盖、变化、实体关联、分类建议/审核和派生视图合同。
- `src/talent-mapping/plan.ts`：版本化 plan、相对 search plan、taxonomy 引用、平台完整性和显式上限校验。
- `src/talent-mapping/workflow.ts`：slice 外层/platform 内层的 scan/enrich 编排、fail-fast、详情 gap 和运行收口。
- `src/talent-mapping/selection.ts`：浏览器工作流与只读 API 共用的确定性详情候选选择和精确计数。
- `src/talent-mapping/change-report.ts` / `entity-links.ts` / `classification.ts` / `quality-service.ts`：历次运行对比、非权威关联线索、受限模型分类建议以及人工确认/撤销/审核。
- `src/talent-mapping/store.ts` / `aggregation.ts` / `export.ts`：JSON/JSONL 事实、checkpoint、含人工审核的派生 view 和 CSV/Markdown 交付。
- `src/rag/jd-question-answering.ts`：JD 检索问答，供 `--jd-question` / `--rag-question` 使用。
- `src/server/http.ts`：内部控制台 HTTP 服务入口，提供 `/api/*` 和前端静态文件。
- `src/server/routes.ts`：控制台 API 路由，覆盖任务提交、任务查询、职位/候选读取、Boss 持久化读取、安全工件、RAG、筛选和助手接口。
- `src/server/api-contracts.ts`：不依赖 Node runtime 的前后端共享 API schema 与类型契约。
- `src/server/boss-read-model.ts`：读取 Boss 职位快照、同步记录、自动沟通审核和幂等回执。
- `src/server/talent-mapping-read-model.ts`：读取 Mapping 项目、运行、人才、公司矩阵、覆盖和质量统计，并生成详情选择预览；不操作浏览器。
- `src/server/talent-mapping-classification-runner.ts`：构建最小化、去身份化的分类输入，调用配置模型并只落盘待人工审核建议。
- `src/server/artifact-read-model.ts`：生成并解析白名单 artifact ID，阻止任意路径读取。
- `src/server/task-normalizers.ts`：HTTP API 和助手确认共用的任务输入校验、模式隔离和 argv 预览。
- `src/server/task-queue.ts`：单进程任务队列，串行执行普通抓取、批量、Talent Mapping、订阅管理、登录刷新、Boss 独立任务和 RAG 运营任务。
- `src/server/task-scheduler.ts`：按每日时间窗口和完成后间隔编排持久化计划，并复用共享任务队列。
- `src/server/cli-assistant.ts`：智能助手草稿生成、字段过滤、缺失字段/风险提示、确认前校验。
- `frontend/src/app/`：BrowserRouter、应用壳、一级导航和全局 provider。
- `frontend/src/api/`：统一 API client、共享契约导出、查询键和浏览器端凭据存储。
- `frontend/src/components/`：安全确认、工件下载、任务输出和通用界面组件。
- `frontend/src/features/`：按控制台、任务、岗位、人才地图、Boss、自动化、知识、助手、设置和新建任务拆分的页面模块。
- `frontend/src/styles/`：视觉 token 与全局响应式样式。
- `src/platforms/registry.ts`：平台注册、默认平台解析与 `--platform` 校验。
- `src/browser/platform-action-context.ts`：无 selector 的页面、deadline、操作名称和剩余时间基础；平台 action 在此之上保留各自 pacing 和 readiness 语义。
- `src/platforms/51job/actions/`：51job 导航、saved/direct 搜索、筛选发现/回放、候选列表、Mapping 批次推进和只读详情的语义入口；新增 Mapping 动作直接归属候选/详情 domain，普通搜索、列表和详情兼容路径仍复用上述旧 `src/browser/` helper。
- `src/platforms/zhilian/actions/`：Zhilian 搜索、筛选、虚拟候选批次、modal 纯详情和分享链接的语义入口；复制链接已由独立 `delivery-actions.ts` 持有，Mapping 只调用纯详情读取，其余多个既有领域实现仍集中在私有 `internal-page-actions.ts`。
- `src/platforms/zhilian/parsing/resume-parser.ts`：Zhilian modal 文本到共享简历的纯转换。
- `src/platforms/liepin/actions/`：Liepin 导航、overlay、saved/direct 搜索、筛选、Mapping 批次、详情和常用联系人转发的语义入口；新增 Mapping 翻页/详情合同保持平台专属 deadline 与失败现场语义，多个既有领域实现仍集中在私有 `internal-page-actions.ts`。
- `src/platforms/liepin/parsing/resume-parser.ts`：Liepin 详情文本到共享简历的纯转换，保留已验证的多段经历边界。
- `src/platforms/boss/actions/context.ts`：Boss 共享 deadline、节奏、连续指针及 native/DOM 兼容点击基础。
- `src/platforms/boss/actions/`：会话读取/变更、简历/转发、人才和职位已按领域组织；普通招聘的登录、搜索/筛选、候选提取和详情打开仍由 `search-actions.ts` 共同持有。动作层不写回执或岗位记录，也不进入任务队列。
- `src/platforms/boss/parsing/resume-parser.ts`：Boss 简历 API/WASM 摘要数据到共享简历结构的纯转换。
- `src/platforms/boss-adapter.ts`：仅保留 `PlatformAdapter` 注册外观和原有兼容导出，实际页面操作委托给动作层。
- `src/platforms/boss-chat.ts`：Boss 自动沟通的快照解析、固定流程组合和兼容导出；控件操作委托给会话与简历动作。
- `src/platforms/boss-talent.ts`：Boss 推荐/深度搜索与打招呼的确认和模式工作流；页面读写由人才动作负责。
- `src/platforms/boss-operations.ts`：Boss 原子会话操作的身份复核、确认、intent 幂等回执和动作编排。
- `src/platforms/boss-jobs.ts`：Boss 职位同步的 JD hash、解析、持久化与运行摘要；职位页面读写由职位动作负责。
- `src/scoring/boss-chat-hard-requirements.ts`：Boss 物业电工严格硬性条件证据判断。
- `src/reporting/boss-chat-summary.ts`：Boss 沟通审查总结渲染与 SMTP 发送。
- `src/search/search-subscription.ts`：独立订阅管理编排。
- `src/reporting/resume-docx.ts`：基于本地 Word 模板渲染单份 DOCX 简历，并处理模板头像位。
- `src/scripts/*.ts`：登录、迁移、重解析、评分、导出、诊断和 smoke 工具。

## 数据模型与落盘结构

普通职位共享类型定义在 `src/types/job.ts`，Boss 独立工作流类型定义在 `src/types/boss.ts`，Talent Mapping 类型定义在 `src/types/talent-mapping.ts`：

- `NormalizedJob`：结构化 JD。
- `JobRecord`：职位持久化记录，包含 `jobKey`、`platform`、`searchKeyword`、JD 原文、标准化 JD 和邮件收件人；Boss 同步岗位还包含职位 ID、状态、同步时间和 JD hash。Boss 原生 saved 设置另存完整 `searchSettings.savedSearch` reference；`RunResult.searchExecution` 保留本轮页面查询、引用和运行时策略摘要。
- `CandidateListItem`：搜索结果页候选人卡片。
- `CandidateResume`：结构化简历，包含基础信息、工作经历、项目经历、教育经历、技能、证书和可选 `candidateShareUrl`。
- `CandidateScoreArtifact`：评分产物，可能是成功评分，也可能是失败记录。
- `RunResult`：一次执行的轻量摘要。
- `BossChatReviewRun`：一次 Boss 未读会话审查的候选人判断、动作状态和错误摘要。
- `BossTalentCandidate` / `BossDeepSearchForm`：Boss 候选人稳定 ID、联系状态，以及深度搜索职位/核心要求/加分项/剩余次数。
- `BossChatOperationResult`：一次原子会话操作的精确会话/候选人信息、只读结果或变更状态和回执路径。
- `BossPositionDetail` / `BossJobSyncRun`：Boss 职位详情和一次同步的 created/updated/unchanged/failed 摘要。
- `JobResultsMarkdownExport`：导出的 Markdown 结果视图。
- `TalentMappingProject`：版本化目标、taxonomy、搜索切片、覆盖上限和 enrichment policy 的持久化项目快照。
- `MappingCandidateObservation` / `MappingProfileObservation`：带 run、slice、平台身份、证据和 observedAt 的卡片/详情事实。
- `MappingRunRecord` / `MappingSliceRun`：stage、平台范围、详情授权、批次/候选/详情数量、终止原因和 gap/failure 语义。
- `MappingCandidateView` / `MappingCompanyRoleMatrixRow` / `MappingCoverageViewRow`：从本地事实可重建的人才、公司岗位矩阵和覆盖视图。
- `MappingEntityLink`：人工确认的跨平台实体关系及可选撤销审计；不会替代平台候选身份。
- `MappingClassificationSuggestion` / `MappingClassificationReview`：模型待审建议、最小证据和人工接受/拒绝记录。
- `MappingRunChangeReport`：两次成功扫描之间的新增、明确字段变化、本轮未再次观察及未变化档案。

默认数据目录是 `data/`，每个职位位于：

```text
data/<platform>/jobs/<jobKey>/
```

目录内容：

- `jd.json`：职位记录，也是 rerun 复用 JD 载荷和邮件收件人的来源。
- `seen-ids.json`：已成功处理的候选人 ID。
- `resumes/<candidateId>.json`：结构化简历。
- `snapshots/<candidateId>.txt`：原始页面文本快照。
- `snapshots-dom/<candidateId>.json`：DOM 抽取快照。
- `scores/<candidateId>.json`：评分产物。
- `results/<timestamp>.json`：轻量运行摘要。
- `exports/latest.md`：最新导出的 Markdown 报告。
- Boss 启用模型要求分流时，`routing/pending-score/` 在简历保存后记录尚未形成决定的精确候选人，并在失败时更新尝试次数和脱敏阶段诊断；`routing/artifacts/` 只追加成功评分后的不可变候选人分类、证据、policy hash 和交付种类，`routing/outbox/` 只保存 qualified/review 的主 Boss 转发状态，`routing/rejection-email-outbox/` 保存 rejected 的候选人级 SMTP 目标、最终正文、首次详情关闭事实和状态。三者与评分产物、seen 分离，不能由任一方替代；任一交付 outbox 一旦存在，对应 pending-score 即可清理。策略版本迁移会把旧策略未完成地址终止为 `superseded`，不再重试，历史 rejected 不自动补发邮件。
- `rag/sources.jsonl`：RAG 事实源记录，包含 JD、历史对话等来源。
- `rag/chunks.jsonl`：RAG chunk 事实源；Qdrant 只是可重建索引。
- `rag/embeddings.jsonl`：RAG embedding 缓存，按 provider、model 和内容 hash 复用向量。
- `rag/conversations/<conversationId>.jsonl`：导入的职位历史对话，按 turn id 去重合并。
- `rag/index-manifest.json`：最近一次 RAG 索引构建摘要。
- `rag/answer-logs.jsonl`：已保存职位的 RAG 问答审计日志，记录 `logId`、问题、答案、confidence、拒答原因、引用 sources 和人工反馈；错误反馈可包含 `errorType`；临时 JD 问答和离线评测不写入。
- `exports/resumes/<candidateId>-<name>.docx`：按 Word 模板离线导出的候选人 DOCX 简历。

Boss 自动沟通、原子会话和职位同步另有独立运行状态：

- `data/boss/chat-review/automation-settings.json`：Boss 自动沟通可复用的默认转发和总结邮件配置。
- `data/boss/chat-review/reviewed-conversation-ids.json`：已完成处理或已成功转发的会话 ID。去重按事件处理：当前仍有新红点的会话即使 ID 已存在也可再次处理；该列表只阻止没有新红点的失败恢复项被重复执行。
- `data/boss/chat-review/runs/<timestamp>.json`：每次审查的候选人 ID、岗位、硬性条件证据、匹配结论、转发/聊天/换电话状态和错误。最新状态仍为 `failed` 且尚未转发的会话可作为重试来源。
- `data/boss/chat-operations/runs/<intentHash>.json`：原子会话变更的输入和成功结果回执；文件名使用 intent ID 的 SHA-256，避免把任意外部字符串直接作为路径。
- `data/boss/job-sync/positions.latest.json`：最近一次读取到的完整 Boss 职位摘要，包括关闭职位。
- `data/boss/job-sync/runs/<timestamp>.json`：每次职位同步各项的创建、更新、未变化或失败结果。

Boss 同步产生的有效 JD 不以 `job-sync/` 快照替代，仍写入 `data/boss/jobs/<jobKey>/jd.json`。同名不同职位 ID 使用不同 `jobKey`；本地 `jd.json` 是后续自动聊天和 RAG 使用的权威职位记录。

Talent Mapping 使用独立项目根目录，不与任何平台岗位目录混放：

```text
data/talent-mapping/<mappingKey>/
├── mapping.json
├── runs/<runId>.json
├── runs/checkpoints/<runId>/
├── platforms/<51job|liepin|zhilian>/
│   ├── card-observations.jsonl
│   ├── profile-observations.jsonl
│   ├── profiles/
│   └── snapshots/
├── annotations.jsonl
├── entity-links.json
├── classification-suggestions.jsonl
├── classification-reviews.jsonl
├── views/{candidates,companies,coverage,changes}.latest.json
└── exports/latest/{candidates.csv,company-role-matrix.csv,coverage.csv,changes.csv,changes.md,summary.md}
```

`card-observations.jsonl`、`profile-observations.jsonl`、人工 annotation、entity links、分类建议和分类审核是本地事实；views 和 exports 可重建。系统不会自动写 entity link，也不会把跨平台相似资料当成同一人；模型建议也不会在人工接受前进入人才/矩阵事实视图。项目配置内容未变化时不重写；每次恢复运行创建新 run ID，失败 run 和已经落盘的 observation/checkpoint 保留。

控制台运行状态独立于职位数据：

- `data/runtime/tasks/`：共享队列的任务记录。
- `data/runtime/schedules/`：自动运行计划定义和当前状态。
- `data/runtime/schedule-runs/`：计划每一轮的任务顺序、完成、取消和失败记录。

历史平台化改造前的数据可通过 `npm run migrate:platform-storage` 从 `data/jobs/` 迁移到 `data/51job/jobs/`，并回填缺失的 `platform` 字段。迁移脚本会拒绝覆盖已存在的目标目录。

## 邮件与失败语义

收件人和抄送列表按职位维度持久化：

- 首次运行传入 `--email` 或 `--cc` 时，会写入对应 `jd.json`。
- 后续同一 `jobKey` 重跑时，如果未显式传入，会复用已保存配置。
- 再次显式传入时，以本次参数为准并覆盖保存值。
- 控制台/API 的显式空 CC 数组表示清空；字段省略才表示复用保存值。

Boss 简历转发和评分/否定邮件是独立交付配置：`bossForwarding.ccEmails` 只控制 qualified/review 的 Boss 页面主转发
副本；`recipientEmail/ccEmails` 控制主聚合报告；`secondaryDelivery.recipientEmail/ccEmails` 控制 rejected 的逐候选
SMTP 邮件。`secondaryForwarding` 和对应 CLI/HTTP/jobs-file 字段已废弃并拒绝。分流开启时，主组为 qualified/review，
rejected 不做 Boss 页面转发，也不发送新的聚合副报告；每个候选人一封邮件，正文包含全部否定原因和完整结构化简历。
`sent`/`uncertain` 邮件不会自动重发；`pending` 或已证明未调用 SMTP 的失败仅在详情关闭事实存在时恢复，且恢复结果即使
候选人已不在当前页面或已被历史 run 收录也会写入本轮状态。只有结构化 `EDNS + CONN` 在当前运行重试一次，第二次失败
标记 `retryExhausted`；AUTH/MAIL 只保留下一轮的一次机会。ETIMEDOUT、ESOCKET、ECONNECTION、其他 CONN、RCPT、DATA、
部分收件人和未知阶段都保持 `uncertain`。同一候选人的 TO/CC 属于一个 SMTP delivery，并在跨进程锁内整体推进状态。
已有运行补发使用
`npm run email:report -- <platform> <jobKey> <recipient> [cc1,cc2] [primary|secondary]`；新 Boss run 的 secondary 会
fail closed 并提示逐候选 outbox，历史旧合同 run 保持只读可审计。

所有报告收件人和抄送地址在 SMTP 调用前执行格式及保留测试域校验；`example.com`、`.test`、`.invalid` 等占位地址
直接失败，不向邮件服务器投递。配置保存和离线测试仍可使用占位地址，但真实发送会 fail closed，避免把测试收件人变成退信。

失败处理：

- 评分失败：保存失败评分产物，候选人仍保持 seen；启用结果分流时保留 pending-score，不创建受众或任何候选级外部交付。
- 上游详情打开或提取失败：不写入 seen，下次继续重试。
- 导出失败：不让整次运行失败，错误记录到 summary。
- 邮件失败：不让整次运行失败，错误记录到 summary；Boss rejected 的每份邮件独立记录 `rejection-email` 处理阶段，结构化且
  证明未提交的 EDNS 失败最多立即重试一次，AUTH/MAIL 只延后一次；第二次失败标记 `retryExhausted`。SMTP 阶段无法证明、
  部分收件人、含糊 CONN 或进程中断记为 `uncertain`，不自动重试。
- 空结果运行：属于成功运行；未开启 Boss 分流时可发送“无新增候选人”通知，开启分流的 Boss run 主、副组都为空时不发送评分报告。
- Zhilian 有新增候选人的报告邮件要求当前运行评分产物都带唯一 `candidateShareUrl`；缺失或重复会导致邮件发送失败并记录到 summary。
- Zhilian 搜索页卡片可能不暴露可用详情链接，候选人 ID 提取会优先读取卡片 Vue props 中的 `userMasterId`、`resumeNumber` 等字段，再使用 API fallback。
- Boss 简历打开或解析失败且尚未转发：记录失败并保留重试来源；即使红点已消失，也可从历史运行恢复当前列表中仍可见的会话。
- Boss 已转发但联系失败：保留 `forwarded: true`，不自动重复转发；总结邮件明确列出联系未完成原因。
- Boss 总结邮件失败：审查运行结果已经落盘，但命令返回失败，避免把未送达误报为成功。
- Boss 立即匹配：没有核心要求、剩余次数为零、按钮不可用或缺少显式确认时，在点击前失败，不消耗匹配次数。
- Boss 单人打招呼：候选人 ID 不存在、姓名/职位复核失败时不点击；已经进入继续沟通状态时返回幂等的已联系结果。
- Boss 原子会话变更：缺少确认/intent ID、会话身份变化、控件不唯一或编辑器已有草稿时不执行；已有相同 intent 回执时返回原结果。
- Boss 职位同步：详情或 JD 解析失败会写入失败同步项，但不覆盖上一份有效 JD；任一项失败时，启用了审查前同步的自动聊天不会继续打开未读会话。
- Talent Mapping 空结果：是成功的 0 候选切片；direct 条件不完整、翻页后批次未变化或身份验证失败则 fail-fast。
- Talent Mapping 受限停止：批次、候选或 deadline 上限以及允许的详情 gap 产生 `completed-with-gaps`，TaskQueue 成功返回但摘要和页面高亮 gaps；不得描述为全量覆盖。
- Talent Mapping 详情失败：卡片事实不删除；51job/Zhilian 记录可重试 gap，Liepin 停止并保留详情现场。`full-detail` 超硬上限时在打开详情前失败。
- Talent Mapping 模式隔离：运行不会创建岗位、改 seen、评分、转发、联系、发送邮件或写 RAG；详情授权只对当前 enrich/all 运行有效。
- Talent Mapping 实体关联：可能关联线索不影响实体数；只有跨至少两个平台且候选均存在的人工确认可生效。冲突关联会拒绝，撤销恢复独立计数并保留确认/撤销审计。
- Talent Mapping 分类审核：模型输出、taxonomy 越界、证据字段/原值不匹配、来源不再是最新观察或建议已无法填补空字段时均不得接受；拒绝记录不改变派生事实。
- Talent Mapping 调度：只有 `card-only` 的 `scan` 可创建自动运行计划；详情阶段或详情能力 plan 在计划入队前失败。

## 维护建议

- 修改 `src/browser/resume-detail.ts` 这类启发式模块时，优先使用已保存快照离线重解析验证，再考虑 live 流程。
- 修改平台搜索入口、列表提取或详情解析时，保留多重 DOM fallback，避免假设页面结构稳定。
- 修改全平台或批量语义时，同时验证单平台、`--platform all` 和 `--jobs-file` 的顺序与错误传播。
- 修改报告或邮件逻辑时，覆盖有新增候选人、无新增候选人、评分失败和 Zhilian 分享链接校验路径。
- 修改 DOCX 简历导出时，保留模板正文、页眉和头像关系的兼容性；头像只能来自候选人本人详情页证据，无法确认时应省略头像而不是使用默认头像、相似人才头像或模板示例头像。

高频行为的源码和回归入口如下，页面 selector、筛选字段数量等易变化细节应以实现和对应测试为准：

| 行为 | 主要实现/约束 | 首选回归 |
| --- | --- | --- |
| CLI 模式、持久化、seen/评分语义 | [`src/index.ts`](./src/index.ts)、[`AGENTS.md`](./AGENTS.md) | [`src/scripts/test-scoring-run-semantics.ts`](./src/scripts/test-scoring-run-semantics.ts) |
| 平台注册、默认节奏和复用浏览器 | [`src/platforms/registry.ts`](./src/platforms/registry.ts)、[`src/browser/AGENTS.md`](./src/browser/AGENTS.md) | [`src/scripts/test-platform-registry.ts`](./src/scripts/test-platform-registry.ts) |
| 全平台语义动作和 facade 边界 | [`src/platforms/`](./src/platforms/)、[`src/platforms/AGENTS.md`](./src/platforms/AGENTS.md) | [`src/scripts/test-platform-action-boundaries.ts`](./src/scripts/test-platform-action-boundaries.ts)、[`src/scripts/test-51job-actions.ts`](./src/scripts/test-51job-actions.ts)、对应平台 adapter 测试 |
| Boss 语义动作上下文 | [`src/platforms/boss/actions/`](./src/platforms/boss/actions/) | [`src/scripts/test-boss-action-context.ts`](./src/scripts/test-boss-action-context.ts)、[`src/scripts/test-boss-action-boundaries.ts`](./src/scripts/test-boss-action-boundaries.ts)、[`src/scripts/test-boss-conversation-actions.ts`](./src/scripts/test-boss-conversation-actions.ts) |
| Boss 自动沟通和物业电工规则 | [`src/platforms/AGENTS.md`](./src/platforms/AGENTS.md)、[`src/scoring/boss-chat-hard-requirements.ts`](./src/scoring/boss-chat-hard-requirements.ts) | [`src/scripts/test-boss-chat.ts`](./src/scripts/test-boss-chat.ts) |
| Boss 推荐、深度搜索和打招呼 | [`src/platforms/boss-talent.ts`](./src/platforms/boss-talent.ts) | [`src/scripts/test-boss-talent.ts`](./src/scripts/test-boss-talent.ts)、[`src/scripts/test-boss-cli-modes.ts`](./src/scripts/test-boss-cli-modes.ts) |
| Boss 原子会话操作和幂等回执 | [`src/platforms/boss-operations.ts`](./src/platforms/boss-operations.ts) | [`src/scripts/test-boss-chat-operations.ts`](./src/scripts/test-boss-chat-operations.ts) |
| Boss 职位/JD 同步和 ID 映射 | [`src/platforms/boss-jobs.ts`](./src/platforms/boss-jobs.ts)、[`src/storage/job-store.ts`](./src/storage/job-store.ts) | [`src/scripts/test-boss-job-sync.ts`](./src/scripts/test-boss-job-sync.ts) |
| Liepin/Zhilian 平台行为 | [`src/platforms/liepin/actions/`](./src/platforms/liepin/actions/)、[`src/platforms/zhilian/actions/`](./src/platforms/zhilian/actions/)、[`src/platforms/AGENTS.md`](./src/platforms/AGENTS.md) | [`src/scripts/test-liepin-adapter.ts`](./src/scripts/test-liepin-adapter.ts)、[`src/scripts/test-zhilian-adapter.ts`](./src/scripts/test-zhilian-adapter.ts) |
| Talent Mapping 合同、存储、工作流、质量审核、CLI/API 和前端确认 | [`src/talent-mapping/`](./src/talent-mapping/)、[`src/types/talent-mapping.ts`](./src/types/talent-mapping.ts) | `npm run test:talent-mapping`、[`src/scripts/test-talent-mapping-quality.ts`](./src/scripts/test-talent-mapping-quality.ts)、[`src/scripts/test-platform-action-boundaries.ts`](./src/scripts/test-platform-action-boundaries.ts) |
| 控制台、助手和自动运行 | [`src/server/AGENTS.md`](./src/server/AGENTS.md) | [`src/scripts/test-server-api.ts`](./src/scripts/test-server-api.ts)、[`src/scripts/test-task-scheduler.ts`](./src/scripts/test-task-scheduler.ts) |
| RAG 事实、检索和质量循环 | [`src/rag/AGENTS.md`](./src/rag/AGENTS.md)、[`docs/rag运营手册.md`](./docs/rag运营手册.md) | 匹配的 `src/scripts/test-rag-*.ts` |
